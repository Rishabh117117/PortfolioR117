/* §4 — Housing Works accent (#C0263B, echoing the AIDS red ribbon) + derived
   shades. Shared by /work/housing-works and its /prototype route so the app
   renders identically in both mounts; set at the page root, everything
   inherits. */
export const HW_ROOT_STYLE = {
  "--accent": "#C0263B",
  "--accent-deep": "#9E1E30",
  "--accent-wash": "#F4D6DB",
  "--accent-tint": "#FBEEF0",
} as React.CSSProperties;
