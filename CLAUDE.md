# Rishabh Salian — Portfolio
Interactive portfolio with live, AI-powered project demos. Deployed on Railway, auto-deploy from main.
## Workflow
- The GitHub repo is the shared source of truth between Rishabh, Claude (chat/planning), and Claude Code (building).
- Claude writes specs into docs/. Claude Code reads them and builds. Railway deploys main.
- ALWAYS read docs/SITEMAP.md, docs/DECISIONS.md, docs/ROADMAP.md before starting any task.
## Hard rules
- ANTHROPIC_API_KEY is server-side only, accessed via app/api/ask. NEVER expose it client-side. Add rate limiting before launch.
- Every demo must be labeled honestly: WORKING (real prototype) vs SIMULATED (scripted). Never overclaim.
- The visual design language is NOT finalized. Do not commit to a design system, palette, or motion library until docs/ROADMAP.md Phase 1 is marked done.
## Projects (11)
- Flagships with live demos: Follow (working prototype), Greener Hours, Low-Carbon Materials, Housing Works.
- Archive (2019–23, static case studies): VSG (shipped client work), OBC Bank, BEST Bus, Music Rooms, YAAP, Stun Gun, Lotus Heater.
- Note: original source files were lost to a dead laptop. BEST app will be rebuilt in code; 3D products (stun gun, lotus heater) remodeled in Blender → GLB; archive imagery extracted from the existing portfolio PDF.

## Changelog
- **FOLLOW-PAGE-1 (2026-06-19) — bespoke `/work/follow`.** Replaced the Phase-3 `[slug]` placeholder with a real Follow page, ported 1:1 from `docs/prototypes/follow-page-scroll.html`. Shipped:
  - `app/work/follow/{page.tsx, FollowReel.tsx, FollowReel.css, follow.module.css}` — hero (dots-mark), deck-style project timeline, the problem, research & interviews, a full-viewport **scroll-pinned pipeline reel** (9 cumulative beats, JS scrubber, caption pill, reduced-motion → final frame), stat strip, differentiators, the four questions, where-it-sits, and the real `DemoCallout`.
  - Routing: `"follow"` added to `BESPOKE_SLUGS` (no double route generation — verified in the build).
  - Accent: Follow Persian Blue `#1C39BB` → burnt orange `#C2410C` in `lib/projects.ts` (+ derived shades on the page root); consistent on the page, the home grid card, and the `/work` card.
  - Reel CSS fully namespaced under `.followReel` (prototype `.reel`→`.followReel`, `pN`→`fpN`, reel-local `--ink`→`--navy`); shell tokens untouched; no global leak.
  - Verified in-browser: build clean (zero TS errors), no console errors, scroll sequencer + scrubber + caption track correctly, mobile stacks (timeline scrolls horizontally), other pages unchanged.
  - **One minimal shell adaptation:** the reel's top caption/label sit at `top:76px` to clear the layout's ~60px sticky nav (the standalone prototype had no shared shell).
  - **Decision flagged (D-03):** demo badge shipped as **SIMULATED** (default — the on-page artifact is an illustrative animation and the sandbox isn't built). To present Follow as **WORKING** instead, flip `status` in `lib/projects.ts`, the Follow page `DemoCallout`, and the home-page `DemoCallout` together.
  - **Follow-ups:** build the team-memory sandbox + point the CTA at it (currently `href="#"`); ESLint isn't installed in the repo, so `npm run lint` would trigger Next's first-run setup — the build's type-check is the gate.
- **HM-PAGE-2 (2026-06-29) — bespoke `/work/healthy-materials` rebuild.** Replaced the 2026-06-25 page (built to the stale 2026-06-17 spec, before the final capstone deck + field photos) with a 10-section narrative on the real content. Spec: `docs/projects/healthy-materials.md`. Shipped:
  - Harvested the keepers: `SwapCard.{tsx,module.css}` (unchanged), the sage/teal/clay `rootStyle`, and the real copy/quotes (Mears ×3, Majeed) — the Theme-3 placeholder is now the real "Process & supply" theme; added the missing leverage-points section + the `GRADUATE RESEARCH · CONCEPT` badge.
  - **Three reused mechanisms, copy-renamed per page (live Follow/Housing Works untouched):** `components/MaterialDeck/` (← `WorkshopDeck`, the moving field-visit carousel, the "Gen Z poster survey" card pattern); `app/work/healthy-materials/AmbientField.{tsx,css}` (← Follow's, recoloured sage-green + clay, with the real `document.hidden` guard the original only promised); and a NEW data-driven `Timeline.{tsx,module.css}` (Follow's deck-style chart, but `at`∈[0,1] data → dates are a `lib/healthyMaterials.ts` edit, not SVG renumbering).
  - Data in `lib/healthyMaterials.ts` (FIELD_DECK ×9, MATERIAL_GRID ×4, TL_* timeline). Deleted dead `Motion.tsx` + `hm-motion.css` (parallax engine with zero targets) → AmbientField is the only motion system now.
  - Assets in `public/images/healthy-materials/`: 9 authentic field photos (EXIF-rotated, resized, see `CREDITS.md`) + 3 web material close-ups from Wikimedia Commons (mycelium CC BY 4.0, hempcrete CC BY 2.0, straw CC0); bloom-foam uses the authentic in-hand card.
  - **Honesty:** two-author capstone (Henry Schroder credited); the research was joint but **Healthy Materials Packages is Rishabh's focus** — flagged as the lead intervention + the SwapCard demo subject. SwapCard stays directional-only (no measured numbers). CC-BY attribution surfaced on-page + in CREDITS.md.
  - Verified: `next build` clean (zero TS errors; static, 5.61 kB; no double route). DOM/computed-style checks pass (orbs z-0/fixed, content z-1, teal/paper rhythm correct, timeline overflow-scroll + 760px min-width, deck marquee + edge-mask, mobile → single column, no horizontal overflow). **Live screenshots blocked by the preview tab's `document.hidden`** → verified via computed-style + a `show_widget` mockup.
  - **Refinements (same day, on review):** hero → **Housing-Works photo-overlay** (library-wall image + scrim + text on top, not teal-with-image-below); materials → a compact **parallax rail** (`MaterialRail.tsx`, 3 short cards with scroll-parallax stagger + hover-lift), **merged into the frame section right under the "exciting materials" hook** (one section, `wrapWide`) — replaced the long teal grid; **better material images** (mycelium-objects + a real hempcrete block, swapped off the "brick home" / plastered-wall shots) — bloom dropped from the grid (stays in the field carousel); timeline corrected to **Fall 2025 only** (SEP–DEC ticks), interview order **Mears → Omir → Ortiz** (Omir before Rick), nodes **even-spaced + "3 interventions"** shortened to fix a label overlap; **AmbientField strengthened** (bigger orbs, ~2× alpha, opacity 0.9/0.62) so the wash reads as intentional, not "dusty paper". Verified live (screenshots + DOM): hero, rail-under-hook, no timeline overlap, stronger wash. `tsc --noEmit` clean.
  - **Refinements round 2 (same day):** hero copy now sits on a **frosted-glass card** (`backdrop-filter: blur(18px)`, scrim lightened so the photo blurs through); **hero re-exported at 1800×2400** from the original (was a soft 1200px full-bleed); **hempcrete image swapped** to a clean finished hemp-lime wall surface (was a confusing grey pile in formwork; CC BY 2.0 credit updated). **AmbientField reworked to be scroll-driven**: each orb traces its own Lissajous path whose phase is advanced by `scrollY` (different X/Y freqs + scroll-loop counts per orb) so they **swirl/travel as you scroll** instead of sitting viewport-fixed ("moon in a train"); a slow time term keeps them alive at rest. Orbs now **fade to ~0.4 over `[data-ambient-dim]` sections** (frame, themes, leverage) for reading legibility. Verified via DOM sampling (orb X swings −97→+123→−138→+137 across scroll; opacity 0.9 vivid ↔ 0.4 over text) + screenshot. `tsc --noEmit` clean.
  - **Refinements round 3 (same day):** **timeline now sits on an end-to-end frosted-glass panel** (`Timeline.module.css .glass`, light translucent paper + `blur(16px)`) so the swirling orbs blur through behind it; footnote/methods stay below. **Three intervention cards made symmetric** — removed the `.conceptFirst { grid-column: span 3 }` so all three are equal 1fr columns side-by-side (kept the green `.conceptFirst` tint as the focus marker; dropped the "↓" from its flag). Verified live (glass blur present; 3 cards same row + 296px each). `tsc --noEmit` clean.
  - **Refinements round 4 (same day):** timeline glass made **full-bleed** (`Timeline.module.css .glass` now `width:100vw; margin-inline: calc(50% - 50vw)`, top/bottom hairlines, no radius — a band, not a box; new `.glassInner` keeps the chart centered at 980; `.page` `overflow-x:clip` absorbs the 100vw, no scrollbar). Under the timeline, the "why these methods" bullets were **replaced with a Follow-style research list** ("The research" → lede → `.rcols` Primary · field engagements (HML field visit + Mears/Majeed/Ortiz) | Secondary · desk research (embodied carbon, codes, Enterprise Green Communities, mindful MATERIALS·HPD, BioFabricate, LEED)); rcols/rlabel/rlist/r/kind CSS ported from Follow. Verified live (full-bleed −7→1273px, no h-scroll, both columns render). `tsc --noEmit` clean.
  - **Open / pending:** exact interview dates if wanted (semester = Fall 2025); confirm Rishabh-vs-Henry split detail; confirm the 37% CO₂ source. **NOT yet committed/pushed** (deploy branch = `claude/*`, NOT `main`).
