# Rishabh Salian — Portfolio
Interactive portfolio with live, AI-powered project demos. Deployed on Railway, auto-deploy from main.
## Workflow
- The GitHub repo is the shared source of truth between Rishabh, Claude (chat/planning), and Claude Code (building).
- Claude writes specs into docs/. Claude Code reads them and builds. Railway deploys main.
- ALWAYS read docs/SITEMAP.md, docs/DECISIONS.md, docs/ROADMAP.md before starting any task.
## Hard rules
- LLM keys are server-side only, accessed via app/api/ask (OPENROUTER_API_KEY preferred, ANTHROPIC_API_KEY fallback; per-IP rate limiting built in). NEVER expose them client-side.
- Every demo must be labeled honestly: WORKING (real prototype) vs SIMULATED (scripted). Never overclaim.
- The visual design language is NOT finalized. Do not commit to a design system, palette, or motion library until docs/ROADMAP.md Phase 1 is marked done.
## Projects (11)
- Flagships with live demos: Follow (working prototype), Greener Hours, Low-Carbon Materials, Housing Works.
- Archive (2019–23, static case studies): VSG (shipped client work), OBC Bank, BEST Bus, Music Rooms, YAAP, Stun Gun, Lotus Heater.
- Note: original source files were lost to a dead laptop. BEST app will be rebuilt in code; 3D products (stun gun, lotus heater) remodeled in Blender → GLB; archive imagery extracted from the existing portfolio PDF.

## Changelog
- **FOLLOW-PAGE-1 (2026-06-19) — bespoke `/work/follow`.** Replaced the Phase-3 `[slug]` placeholder with a real Follow page, ported 1:1 from `docs/prototypes/follow-page-scroll.html`. Shipped:
  - `app/work/follow/{page.tsx, FollowReel.tsx, FollowReel.css, follow.module.css}` — hero (dots-mark), deck-style project timeline, the problem, research & interviews, a full-viewport **scroll-pinned pipeline reel** (9 cumulative beats, JS scrubber, caption pill, reduced-motion → final frame), stat strip, differentiators, the four questions, where-it-sits, and the real `DemoCallout`.
  - Routing: `"follow"` added to `BESPOKE_SLUGS` (no double route generation — verified in the build).
  - Accent: Follow Persian Blue `#1C39BB` → burnt orange `#C2410C` in `lib/projects.ts` (+ derived shades on the page root); consistent on the page, the home grid card, and the `/work` card.
  - Reel CSS fully namespaced under `.followReel` (prototype `.reel`→`.followReel`, `pN`→`fpN`, reel-local `--ink`→`--navy`); shell tokens untouched; no global leak.
  - Verified in-browser: build clean (zero TS errors), no console errors, scroll sequencer + scrubber + caption track correctly, mobile stacks (timeline scrolls horizontally), other pages unchanged.
  - **One minimal shell adaptation:** the reel's top caption/label sit at `top:76px` to clear the layout's ~60px sticky nav (the standalone prototype had no shared shell).
  - **Decision flagged (D-03):** demo badge shipped as **SIMULATED** (default — the on-page artifact is an illustrative animation and the sandbox isn't built). To present Follow as **WORKING** instead, flip `status` in `lib/projects.ts`, the Follow page `DemoCallout`, and the home-page `DemoCallout` together.
  - **Follow-ups:** build the team-memory sandbox + point the CTA at it (currently `href="#"`); ESLint isn't installed in the repo, so `npm run lint` would trigger Next's first-run setup — the build's type-check is the gate.
- **HM-PAGE-2 (2026-06-29) — bespoke `/work/healthy-materials` rebuild.** Replaced the 2026-06-25 page (built to the stale 2026-06-17 spec, before the final capstone deck + field photos) with a 10-section narrative on the real content. Spec: `docs/projects/healthy-materials.md`. Shipped:
  - Harvested the keepers: `SwapCard.{tsx,module.css}` (unchanged), the sage/teal/clay `rootStyle`, and the real copy/quotes (Mears ×3, Majeed) — the Theme-3 placeholder is now the real "Process & supply" theme; added the missing leverage-points section + the `GRADUATE RESEARCH · CONCEPT` badge.
  - **Three reused mechanisms, copy-renamed per page (live Follow/Housing Works untouched):** `components/MaterialDeck/` (← `WorkshopDeck`, the moving field-visit carousel, the "Gen Z poster survey" card pattern); `app/work/healthy-materials/AmbientField.{tsx,css}` (← Follow's, recoloured sage-green + clay, with the real `document.hidden` guard the original only promised); and a NEW data-driven `Timeline.{tsx,module.css}` (Follow's deck-style chart, but `at`∈[0,1] data → dates are a `lib/healthyMaterials.ts` edit, not SVG renumbering).
  - Data in `lib/healthyMaterials.ts` (FIELD_DECK ×9, MATERIAL_GRID ×4, TL_* timeline). Deleted dead `Motion.tsx` + `hm-motion.css` (parallax engine with zero targets) → AmbientField is the only motion system now.
  - Assets in `public/images/healthy-materials/`: 9 authentic field photos (EXIF-rotated, resized, see `CREDITS.md`) + 3 web material close-ups from Wikimedia Commons (mycelium CC BY 4.0, hempcrete CC BY 2.0, straw CC0); bloom-foam uses the authentic in-hand card.
  - **Honesty:** two-author capstone (Henry Schroder credited); the research was joint but **Healthy Materials Packages is Rishabh's focus** — flagged as the lead intervention + the SwapCard demo subject. SwapCard stays directional-only (no measured numbers). CC-BY attribution surfaced on-page + in CREDITS.md.
  - Verified: `next build` clean (zero TS errors; static, 5.61 kB; no double route). DOM/computed-style checks pass (orbs z-0/fixed, content z-1, teal/paper rhythm correct, timeline overflow-scroll + 760px min-width, deck marquee + edge-mask, mobile → single column, no horizontal overflow). **Live screenshots blocked by the preview tab's `document.hidden`** → verified via computed-style + a `show_widget` mockup.
  - **Refinements (same day, on review):** hero → **Housing-Works photo-overlay** (library-wall image + scrim + text on top, not teal-with-image-below); materials → a compact **parallax rail** (`MaterialRail.tsx`, 3 short cards with scroll-parallax stagger + hover-lift), **merged into the frame section right under the "exciting materials" hook** (one section, `wrapWide`) — replaced the long teal grid; **better material images** (mycelium-objects + a real hempcrete block, swapped off the "brick home" / plastered-wall shots) — bloom dropped from the grid (stays in the field carousel); timeline corrected to **Fall 2025 only** (SEP–DEC ticks), interview order **Mears → Omir → Ortiz** (Omir before Rick), nodes **even-spaced + "3 interventions"** shortened to fix a label overlap; **AmbientField strengthened** (bigger orbs, ~2× alpha, opacity 0.9/0.62) so the wash reads as intentional, not "dusty paper". Verified live (screenshots + DOM): hero, rail-under-hook, no timeline overlap, stronger wash. `tsc --noEmit` clean.
  - **Refinements round 2 (same day):** hero copy now sits on a **frosted-glass card** (`backdrop-filter: blur(18px)`, scrim lightened so the photo blurs through); **hero re-exported at 1800×2400** from the original (was a soft 1200px full-bleed); **hempcrete image swapped** to a clean finished hemp-lime wall surface (was a confusing grey pile in formwork; CC BY 2.0 credit updated). **AmbientField reworked to be scroll-driven**: each orb traces its own Lissajous path whose phase is advanced by `scrollY` (different X/Y freqs + scroll-loop counts per orb) so they **swirl/travel as you scroll** instead of sitting viewport-fixed ("moon in a train"); a slow time term keeps them alive at rest. Orbs now **fade to ~0.4 over `[data-ambient-dim]` sections** (frame, themes, leverage) for reading legibility. Verified via DOM sampling (orb X swings −97→+123→−138→+137 across scroll; opacity 0.9 vivid ↔ 0.4 over text) + screenshot. `tsc --noEmit` clean.
  - **Refinements round 3 (same day):** **timeline now sits on an end-to-end frosted-glass panel** (`Timeline.module.css .glass`, light translucent paper + `blur(16px)`) so the swirling orbs blur through behind it; footnote/methods stay below. **Three intervention cards made symmetric** — removed the `.conceptFirst { grid-column: span 3 }` so all three are equal 1fr columns side-by-side (kept the green `.conceptFirst` tint as the focus marker; dropped the "↓" from its flag). Verified live (glass blur present; 3 cards same row + 296px each). `tsc --noEmit` clean.
  - **Refinements round 4 (same day):** timeline glass made **full-bleed** (`Timeline.module.css .glass` now `width:100vw; margin-inline: calc(50% - 50vw)`, top/bottom hairlines, no radius — a band, not a box; new `.glassInner` keeps the chart centered at 980; `.page` `overflow-x:clip` absorbs the 100vw, no scrollbar). Under the timeline, the "why these methods" bullets were **replaced with a Follow-style research list** ("The research" → lede → `.rcols` Primary · field engagements (HML field visit + Mears/Majeed/Ortiz) | Secondary · desk research (embodied carbon, codes, Enterprise Green Communities, mindful MATERIALS·HPD, BioFabricate, LEED)); rcols/rlabel/rlist/r/kind CSS ported from Follow. Verified live (full-bleed −7→1273px, no h-scroll, both columns render). `tsc --noEmit` clean.
  - **Open / pending:** exact interview dates if wanted (semester = Fall 2025); confirm Rishabh-vs-Henry split detail; confirm the 37% CO₂ source. **NOT yet committed/pushed** (deploy branch = `claude/*`, NOT `main`).
- **SHELL-1.1 + ABOUT-PAGE-1 (2026-07-01) — About built for real; the flagship design DNA goes site-wide.** Per Rishabh (base: the approved sandbox `about me page.html`; the supplied CV PDF matched its work history but is outdated — D-05 stays open):
  - **Tokens v1.1** (`app/globals.css`, additive only): `--shadow-rest`/`--shadow-lift`, `--glass-bg`/`--glass-blur`, `--nav-h: 60px`; global `.kicker` (mono-gold section heading) + `.lift` (opt-in hover-lift). Default accent stays Persian Blue.
  - **Shared `components/AmbientField`** (HW-generation engine — scroll-phase Lissajous, `[data-ambient-dim]`, `document.hidden` guard — parameterized by orb `{color, alpha}` ×4 + rest opacities): mounted on `/` + `/about` (gold→blue), `/archive` (gold-led), and `/archive/[slug]` (**project accent + gold** per project). The four flagship pages keep their own copies, untouched. **Shared `components/DriftGroup`** (MaterialRail parallax generalized, `data-depth` children; hover stays on inner elements).
  - **`/about` rebuilt** (resolves D-04): thesis hero + placeholder photo cluster (kept as placeholders, they drift on scroll), journey SVG timeline (fonts via CSS classes — next/font hashes family names, literal names in SVG attrs would fall back), CV-as-work-list with pill CTAs into /work + /archive, skills panels (`.lift`), contact (GitHub/Behance live; CV download disabled pending D-05). Accent = global Persian Blue, no page override.
  - **Home reworked onto v1.1**: ambient + z-layered content, hero kicker accent-blue → gold, arc band frosted glass, arc words + earlier-work grid drift, card hovers → `--shadow-lift`. Copy untouched (D-01 open).
  - **`components/Breadcrumbs`** mounted in `layout.tsx` under `<Nav/>`: mono trail on every route except `/`; **non-sticky by design** (the Follow reel pins captions assuming ~60px chrome); labels from `lib/projects.ts`. **Footer** contact links went live (Email/GitHub/Behance).
- **CV-DOWNLOAD-1 (2026-07-02) — removed the `/cv` route (resolves D-05).** Per Rishabh the CV lives as a **direct PDF download** from the About contact block (`public/Rishabh-Salian-CV.pdf`, the supplied file). Deleted `app/cv/`; dropped the CV nav link (Work · About · Contact); the About "Download CV · PDF" button is now a live `<a download>`; the home CTA "View CV" → "About & CV" points at `/about`. To refresh: drop a new PDF at that public path — no code change.
- **GH-DECK-1 → GH-PAGE-1 (2026-06-30) — bespoke `/work/greener-hours`.** First shipped as the finished presentation deck embedded in a 16:9 iframe (GH-DECK-1, deck vendored verbatim to `public/greener-hours/index.html`). Then, per Rishabh, **rebuilt as a full narrative case study** (GH-PAGE-1) — a peer to Follow/HW/Healthy Materials — translating the deck's content + visual language. Spec: `docs/projects/greener-hours.md`. Shipped:
  - **Hybrid look:** portfolio shell + Bricolage, a **page-scoped serif (Fraunces, `--font-serif`)** loaded via `next/font` in the page only (display moments), and the deck's **amber `#C2410C` / navy `#1E3A5F`** as **page-scoped vars on the root** (never global) carried into the diagrams. Primary accent stays **Forest `#1C3B36`** (matches the card; dodges Follow's amber). Semantics: amber = carbon, navy/forest = clean.
  - **Faithful deck diagrams rebuilt as SVG/React:** `ScaleChart` (945 TWh bar chart), `VisibilityFlow` (dark-matter flow, dark band), `ForceVisual` (3 forces), `HeadersDiagram` (3 API headers → 3 tiers), `AdoptionCurve` (HTTPS, dark band) + shared `diagrams.css` utility classes. **CURATED product:** `SurfaceThumbs` (3-surface overview, deck slide 7) + a full **`ComputeWindowMock`** Tier-1 chat carbon-indicator (the Claude-in-the-chat hook); Tiers 2/3 as the lighter thumbnails.
  - **Sections:** hero → scale → invisibility → insight → 3 forces → one standard/3 surfaces + substrate → how-it-works → Tier-1 deep dive → HTTPS precedent → 6 precedents → 4 trade-offs → 6 KPIs (+ directional caveat) → close → DemoCallout → pager. 3 dark navy bands (§ invisibility, HTTPS, close). Data in `lib/greenerHours.ts`. `AmbientField` copy-renamed (forest+amber, namespaced `gh-`).
  - **Honesty:** badge `COURSE PROJECT · CONCEPT` (in `lib/projects.ts` status + the hero), `DemoCallout` SIMULATED, illustrative/source captions on the mock + figures. The **vendored deck is kept** and linked "View the full deck ↗". Interactive Tier-1 prototype + `/api/ask` wiring deferred (Phase 4 → LIVE API).
  - **Verified:** `npm run build` clean (20/20 static; `/work/greener-hours` 3.5 kB; no double route). In-browser: Fraunces scoped (doesn't leak to other routes), scoped tokens resolve, all diagrams render, dark bands legible, no console errors, no h-overflow desktop + mobile. **Committed per-file** (deploy branch `claude/*`).
  - **Restructure (same day, per Rishabh):** reordered the narrative so the **HTTPS precedent + 3 supporting precedents sit right after the reframe** (combined into one dark "the reframe, proven" section), then opportunity → product; restyled the reframe statement (cramped quote → clean full-width opener); and rebuilt the product as the **three surfaces in a tabbed view** — `TierTabs` (client, WAI-ARIA tablist + roving keys) wrapping full mockups for all three tiers (`ComputeWindowMock` T1, new `SchedulerMock` T2, new `DashboardMock` T3; shared `TierMocks.module.css`). Removed `SurfaceThumbs`. Build clean (8.54 kB, 20/20); DOM-verified (flow order, tabs switch, no overflow/console errors); 3-lens review fixes applied (stable tabpanel id, caret color, defensive min-width, dropped unused `--amber-wash`). Screenshots: the preview screenshot tool hung this session — verified via eval/DOM.
  - **Come-alive (same day, per Rishabh + the supplied `greener-hours-prototype.jsx`):** the three tier surfaces are now **LIVE** (prototype logic adapted into the page's tokens, no new deps). **Implemented `app/api/ask`** as a server-side Claude proxy (per-IP rate limiting; `ANTHROPIC_API_KEY` stays server-side — was a 501 scaffold) and wired the **Tier-1 chat** to it; **Tier-2** is a working scheduler queue (submit → cleanest-window routing → run/complete on the sim clock); **Tier-3** is a ticking live dashboard. `DemoCallout` SIMULATED → **WORKING** (resolves D-03 for GH; project framing stays `COURSE PROJECT · CONCEPT`). Background rhythm reworked: §3+§4 linked on one dark band, §5 reframe centered/light, §6 opportunity on a blue band; the P-codes are noted as the course's ten principles (SP26 syllabus). **Prod chat requires `ANTHROPIC_API_KEY` in Railway** (without it, `/api/ask` 503s gracefully and the chat says so; everything else stays interactive). Build clean (10.1 kB; `/api/ask` server route); verified live (chat 503-graceful sans key, scheduler/dashboard interactive, no console/overflow, desktop+mobile).
- **HM-PROTO-1 (2026-07-02) — Healthy Materials Packages became a working web app; `/api/ask` went multi-provider (OpenRouter).** Per Rishabh: prototypes must "feel like proper web applications … built on mock data but simulating the real thing very accurately", with OpenRouter for LLM calls.
  - **`/api/ask` is provider-agnostic now:** prefers **OpenRouter** (`OPENROUTER_API_KEY`; model via `OPENROUTER_MODEL`, default `anthropic/claude-haiku-4.5` — verified against OpenRouter's public /models list) with the direct Anthropic path (`claude-sonnet-4-6`) as fallback. **Demo-keyed server-side system prompts** (`greener-hours` = default for back-compat, `hm-packages`) so clients can never write instructions; optional bounded `context` string (≤2,400 chars) is appended as data-not-instructions for grounding; per-IP rate limiting kept. `.env.example` reworked (+`SITE_URL` attribution referer). GH chat unchanged (no `demo` field → GH prompt); GH greeting + DemoCallout copy de-branded to "a real/live model API" since the provider may be either.
  - **The prototype (`PackagesApp.tsx` + `PackagesAssistant.tsx` + `SpecSheetModal.tsx` + `PackagesApp.module.css`):** a browser-framed 3-region app (scope rail + live totals · line cards · assistant dock) over **`lib/hmPackages.ts`** — 4 scopes (unit renovation / corridor / lobby / bathroom), 20 lines, each BAU-vs-swap with installed $/unit, A1–A3 kgCO₂e/unit, VOC class, real-world cert vocabulary (HPD · Declare · GreenGuard Gold · NAF · CDPH v1.2 · EGC ties), regional supplier count + lead time (the Theme-3 supply-fragility signal), and a VE read (`survives`/`watch`) + defense line. Numbers are internally consistent and story-bearing: unit reno lands **+3.1% first cost / −50% carbon** (the "survives value engineering" flagship); corridor is deliberately the hard case (**+8.7%**, two watch lines, maintenance-math defenses — Theme 2 made tangible).
  - Interactions: per-line BAU↔swap tiles recompute totals live (bars share one denominator); **cost-pressure lens** sorts VE-watch lines first + reveals their defenses; **spec-sheet modal** (`createPortal` to body — the HW z-trap — z-1200 over nav, Esc + focus handling) with **CSV download + copy summary**; **assistant** posts `demo:"hm-packages"` + the current package state as context and degrades to a labeled offline state without a key. Honesty strip on the app + sheet: "illustrative data — representative magnitudes, not measured quotes or verified EPDs" (supersedes §9's direction-only rule per Rishabh's mock-data directive; spec doc updated).
  - Page: §9 rebuilt around the app on a **full-bleed band** (`.appBleed`, 1280 inner; `.page` overflow-x clip absorbs the 100vw); `DemoCallout` **WORKING** → new standalone **`/work/healthy-materials/prototype`** route (shared sage accent via `theme.ts`, which replaced the inline rootStyle). **SwapCard deleted** (superseded; history in git + the spec doc).
  - Verified: `next build` clean (19/19; page 5.26 kB, prototype 304 B, `/api/ask` dynamic). In-browser: totals match hand-calcs; toggles/scope-switch/lens/modal/offline-assistant all pass; mobile stacks with zero h-overflow; `/work/greener-hours` regression clean (3 tabs, chat hits the new route gracefully); no console errors; OpenRouter endpoint + default model probed live (401 unauthenticated / model present). **Prod assistant needs `OPENROUTER_API_KEY` (or `ANTHROPIC_API_KEY`) on the Railway service** — without it the app stays fully interactive and the assistant shows an honest offline state.
- **HW-PROTO-1 (2026-07-02) — Trustee Workshops became a working web app (the Housing Works prototype).** Arc 2 of the proper-web-application directive (after HM-PROTO-1, same session):
  - **`WorkshopsApp` (+ `WorkshopsAssistant`, `WorkshopsApp.module.css`)** replaces the scripted `WorkshopHarness` in scene 9: a browser-framed internal People-team tool (`workshops.housingworks.internal/people`) with three views on **`lib/hwWorkshops.ts`** — **Match** (6-need pulse-survey queue with votes/dept/priority → an **explainable matcher** scoring every trustee in the open: skill fit 55 + strategic fit 25 + capacity 20, with per-component bars and a plain-language "why"; a real computation, labeled as such), **Sessions** (scheduled 45-min workshops with the 5/15/15/10 agenda; ● Run plays the capture loop live — transcript turns → auto-summary → tags + badge → "Archive to team memory"), and **Archive** (4 seeded + new sessions; full-text search + tag filters). Program stats (sessions/badges/avg rating/**$-of-$900 budget bar** — the page's "$2,700/yr for all three" hook) update live, and scheduling feeds trustee **load back into the matcher**.
  - **Assistant = "Ask the archive"** (`/api/ask`, new demo prompt `hw-workshops`): grounded on the live program state — bench, queue, open needs, every archived summary — the "what's taught stays with the team" promise made real (and the Follow thesis rhyme). Graceful labeled offline state without a key.
  - Page: scene 9 rebuilt — convergence diagram → intro copy → 45-min timeline SVG (moved above the app) → the app on a **full-bleed band** (`.appBleed`, 1280 inner) → `DemoCallout` **WORKING** → new standalone **`/work/housing-works/prototype`** (crimson accent via shared `theme.ts`, replacing the inline rootStyle). **`WorkshopHarness` deleted**; `lib/housingWorks.ts` pruned to the poster deck + bridges data (harness synthetics superseded by `lib/hwWorkshops.ts`; dead `POSTER_FOOTNOTE` dropped); its stale live-path (old `{prompt}` API shape) is gone with it. `lib/projects.ts` HW status `SIMULATED` → **`STUDIO PROJECT · CONCEPT`** (framing badge; demo status lives on the callout — resolves D-03 for Housing Works: WORKING).
  - **Honesty:** on-product strip — "trustees, needs, and sessions are invented stand-ins for private Housing Works data"; the matcher footnote says the score is computation, not model output; the callout reiterates "Housing Works hasn't piloted the framework — this is the proposal, made operable" (consistent with scene 10's limitations).
  - Verified: `next build` clean (20 routes; HW page 5.94 kB — lighter than the harness build — prototype 307 B). In-browser: matcher scores match the formula (Maria 93/45/23/17 for merchandising, correct whys), full loop passes (schedule → need leaves queue + load bumps → run → 5-turn playback → summary → archive entry highlighted first → stats/budget bump $180→$225), search + tag filters work, assistant offline-graceful, mobile stacks w/o overflow, prototype route + breadcrumbs OK, zero console errors. **Same Railway env note as HM-PROTO-1: the assistant needs `OPENROUTER_API_KEY` (everything else stays interactive without it).**
- **FOLLOW-DEMO-1 + GH-SIM-1 + FREE-MODELS-1 (2026-07-02) — the Follow sandbox went live, Greener Hours became one product, and the LLM chain went free-first.** Arc 3 of the proper-web-application session, per Rishabh ("live demo of follow… make greener hours better… free models or pro models under a dollar or 2 max"):
  - **FREE-MODELS-1 — `/api/ask` cost policy:** OpenRouter requests now use the **`models` fallback-routing array**: `openai/gpt-oss-120b:free` → `meta-llama/llama-3.3-70b-instruct:free` → `google/gemini-2.5-flash-lite` ($0.10/$0.40 per MTok — the only paid link, well under the $1–2 cap; chain verified against OpenRouter's live /models pricing). Free-tier flakiness (429s) auto-fails-over server-side. `reasoning: {effort: "low"}` keeps gpt-oss from burning the 700-token budget thinking; empty-content-with-reasoning responses are tolerated. `OPENROUTER_MODEL` now *prepends* an override primary. The direct-Anthropic fallback dropped `claude-sonnet-4-6` → **`claude-haiku-4-5`** (cheapest tier; that path only runs without an OpenRouter key). Context cap 2,400 → **3,200 chars** (the Follow memory needs it).
  - **FOLLOW-DEMO-1 — the team-memory sandbox (resolves the D-02 placeholder CTA + D-03 for Follow: demo = WORKING).** `lib/followSandbox.ts` + `app/work/follow/{FollowSandbox,FollowAskDock}.tsx` + module css: a pre-loaded workspace ("Aurora — checkout redesign") — **Maya·Claude / Alex·ChatGPT / Sam·Gemini** (tool-colored avatars carry the "between your AI tools" story), **20 memory entries** each with provenance (who · tool · thread · day) and kind (decision/finding/constraint), **2 genuinely contested pairs** (Stripe-vs-Adyen fees, guest-vs-account checkout) flagged "Follow keeps both sides on the record" instead of resolved; searchable memory + topic chips + contested-only toggle; a **who-knows-what directory** (the TMS pitch) derived live with cross-links back into the memory; and **Ask Follow** (`/api/ask` demo `follow`) — the server-side prompt mandates per-answer attribution and explicit contradiction-surfacing. Page: the placeholder callout section became kicker + intro + full-bleed sandbox + **WORKING** DemoCallout → new **`/work/follow/prototype`**; accent moved to `theme.ts`; `lib/projects.ts` Follow `SIMULATED` → **`CAPSTONE · WORKING DEMO`** (framing badge; no home callout exists anymore, so the two flip points stayed in sync).
  - **GH-SIM-1 — Greener Hours is one product now.** New `app/work/greener-hours/GhSim.tsx` (context provider mounted by `TierTabs`, above the tabs, so state survives switches): ONE 24h grid + ONE sim clock (auto-ticks 8s, `document.hidden`-paused) + ONE flexible-job queue shared by all three tiers. **Pause the T2 scheduler and the T1 indicator pill freezes; submit a job in T2 and T3's new `flexStrip` reports it** (count · completions · avg −% intensity · ~g CO₂eq avoided, labeled sim); the dashboard's AVG INTENSITY now reads the same clock as the pill instead of a random walk. **T1 chat is grounded**: sends the live sim state (hour, intensity, label, cleanest-of-next-12h, queue) as context — it can answer "when's the cleanest window today?" from the forecast it sits on — plus 3 suggestion chips. `SchedulerMock` rewritten on the shared sim (dup grid/clock code deleted). DemoCallout CTA: deck link → **`/work/greener-hours/prototype`** (new full-screen route, `theme.ts` for the scoped tokens; the deck stays linked in the hero); body copy now tells the cross-tier story.
  - Verified: `next build` clean (22 routes; GH page 10.1 → 2.5 kB — the tier code moved into a shared chunk with the new route). In-browser: Follow page reel + both compare diagrams intact; sandbox filters/search/directory-cross-link/contested-toggle/ask-offline all pass; **cross-tier proof live** (pause froze the pill at the same value the dashboard showed — 444 g at 16:00 — and the submitted job appeared on the flex strip: "3 jobs routed · avg −49% · ~1124 g avoided"); all four /work chips render (`CAPSTONE · WORKING DEMO` / `COURSE PROJECT · CONCEPT` / `GRADUATE RESEARCH · CONCEPT` / `STUDIO PROJECT · CONCEPT`); both new prototype routes + breadcrumbs OK; mobile stacks, zero console errors. **Railway env unchanged: everything degrades gracefully until `OPENROUTER_API_KEY` is set.**
- **FOLLOW-MCP-1 (2026-07-02) — the MCP console: watch Follow's actual tools work.** Per Rishabh ("place a chat where people can see how the MCP tools work… refer to the follow repo… based on the actual code… you can too use follow since it's added as a custom connector"):
  - **Grounded in the real code + the live server.** Tool names, descriptions, and input schemas were lifted from the shipped MCP implementation (workspace-platform repo, `packages/api/src/mcp/tools/*.ts` — 12 tools over JSON-RPC at `/mcp`, headless: user and machine callers). Response shapes were verified TWO ways: reading the handlers, and **calling the live Follow connector from this session** (query_index / directory_query / get_activity) — the sandbox's directory JSON mirrors a real response field-for-field (userId/email/totalFacts/sessionCount/topicsCovered/stateChainDepth/…), query_index emits the real `Found N result(s) for "q":` + `**title** (NN% match)` format, save_conversation returns the real `Saved N messages from {source} as "{title}" (id: …, version 1). Intelligence pipeline running in the background.` Params the sandbox can't honor (Boundary[] governance, structured query mode) were trimmed rather than silently ignored.
  - **`lib/followMcp.ts`:** the 5-tool registry in OpenAI function format (`query_index`, `directory_query`, `detect_contradictions`, `get_activity`, `save_conversation`) + a faithful executor over the sandbox workspace — including the WRITE path: `save_conversation` adds a real entry (contributor "You", visible in Team memory, the team rail, and the directory). Unit-verified via tsx (all shapes + write→read round trip: a saved claim ranks first in the next query_index).
  - **`/api/ask` gained a real tool-calling mode** (`demo:"follow-mcp"`): the tool registry stays SERVER-side (clients can never define tools); an extended sanitizer passes OpenAI-format `tool_calls` / `role:"tool"` turns through with hard caps + name allow-listing; responses return `{toolCalls}` or `{text}`. The whole free-first model chain supports `tools` (verified via OpenRouter `supported_parameters`). MCP mode is OpenRouter-only (the Anthropic fallback speaks a different tool dialect — returns a labeled 503 instead).
  - **`McpConsole.tsx`** = the third view in the Follow sandbox ("MCP console · 5"): the model decides the calls; each `⚙ tool` card renders args JSON + the result on the wire; the client runs the loop (≤4 rounds) against the executor and the final answer is grounded in what came back. Tool legend with schema tooltips + **"source: workspace-platform ↗"** repo link (repo checked public). Stays mounted across view switches so the wire survives hopping to Team memory to see a saved entry. Suggested prompts incl. the save flow. Page intro + WORKING callout updated to name the console.
  - Verified: build clean (22/22); executor unit tests; in-browser full-loop harness (scripted model, real everything else) — proper `user → assistant+tool_calls → tool,tool → …` threading across 3 rounds, tool cards render, **memory pill ticked 20→21 live**, "You · 1" joined the team rail, saved entry newest-first in Team memory; real offline path shows the MCP-specific 503 note; zero console errors. The model-driven loop end-to-end needs `OPENROUTER_API_KEY` (free chain, tools-capable) — same single env var as everything else.
- **WORLD-READY-1 (2026-07-03) — the publication layer + full-site review fixes.** From the graded site review (`Desktop\Claude\REVIEW-PORTFOLIO-2026-07-03.md`: B overall — "A-grade product, D-grade publication layer"); executes review sessions 1–3 + strategy items:
  - **SEO/share pack:** root `metadataBase` (SITE_URL env, Railway fallback — single source `lib/site.ts`) + `title.template` (13 page titles normalized to plain strings) + openGraph/twitter blocks; **`public/opengraph-image.png`** (1200×630, satori-shaped real Bricolage + ambient washes + the 4-accent dots, 61KB); **`app/icon.svg`** (2×2 flagship-accent dots) + `app/apple-icon.png`; **`app/robots.ts`** (disallow /api/, /greener-hours/) + **`app/sitemap.ts`** (generated from FLAGSHIPS+ARCHIVE); JSON-LD Person in layout. Branded **`app/not-found.tsx`** + **`app/error.tsx`** (the defaults were a bare black screen inside the cream shell). **Set `SITE_URL` on Railway; custom domain still open.**
  - **A11y:** `--gold` #9a7b4f→**#7a5f3c** (kickers were 3.5:1 on paper — AA fail; decorative orb golds keep the light hex via props), HM sage accent #5C7A3A→**#4F6B33** (link contrast); skip-to-content link in layout (+`.skipLink` in globals); `aria-live="polite"` on all four chat threads (FollowAskDock/PackagesAssistant/WorkshopsAssistant/ComputeWindowMock — McpConsole already had it); GhSim auto-tick now respects `prefers-reduced-motion`; DashboardMock interval got the missing `document.hidden` guard.
  - **API hardening (`app/api/ask`):** `AbortSignal.timeout(30s)` on both upstream fetches (timeout → 504); rate-limit IP = `x-real-ip` else **last** XFF hop (first hop is client-spoofable) + stale-entry eviction (cap 500); content-length gate 64KB → 413; upstream error bodies + thrown-fetch causes now **log server-side only** (clients get generic messages — the em-dash-debuggability lives in Railway logs); env-var names stripped from all user-facing error strings; grounded demo prompts (follow/hm/hw) now **refuse-not-fabricate when no context is attached** (a live probe without context had the model inventing teammates); "(no response)" → friendlier fallback.
  - **Copy trust batch:** Follow eyebrow **"PGDM Design Strategy" → "MS Strategic Design & Management"** (was the site's most visible error); the 6-of-8-vs-5-participants contradiction neutralized (**"most participants"** — restore the real n when Rishabh confirms it); Lockheed line requalified ("student concept … speculative brand treatment"); "Act on"→"Acts on"; health centre→center; specialise→specialize; data-centre→data-center; lotus-heater "exisiting"→"existing" label fixed (also patch `_asset-tools/export_archive.mjs` if regenerating); HW meta description un-garbled; 52/23/6 → "52 hybrid · 23 remote · 6 in-person" (confirmed against lib data); HM "Capstone" → **"Research capstone"** (differentiates from Follow's); HM close tense ("Next: the … interviews, then the final affinity mapping"); About: h1 → **"I build AI-native products end to end."** (no longer duplicates the home h1), "University of Mumbai", date ranges → closed en dashes; "package vs bau"→BAU; Spring 2025 caps.
  - **Runtime:** `MaterialRail` rebuilt on shared `DriftGroup` (its bespoke engine leaked self-breeding rAF chains on every scroll event — verified live: 3 cards drift, leak gone); **31 JPGs recompressed in place** (EXIF-aware, ≤1800px long edge, mozjpeg q74; `public/images` non-archive 11.1MB→6.4MB; HM hero 848→293KB, HW dividers ~halved); **`sharp` installed** (Railway image optimizer was on the WASM fallback).
  - **Strategy/nav:** shared **`components/ProjectPager`** (wrap-around ← →) mounted on ALL four flagships (only GH had one; its inline block replaced — the deep-dive flow no longer dead-ends); home hero gained the **availability chip** ("● Open to Design Engineer & Product roles — Brooklyn, NY", mirrors About); **one-line taglines** on the four flagship cards (`lib/projects.ts tagline` + ProjectCard support); the circular "All work →" self-link dropped; GH page now imports `GH_ROOT_STYLE` from `./theme` (was a hand-synced duplicate) and loads **Fraunces as a variable font** (2 files, all weights — the tier numerals' `font-weight:300` finally renders as real Fraunces 300; 700 dead weight gone).
  - **Hygiene:** purged ~3.2K dead lines (`components/{Parallax,Openings,Scene,OffsetCard}`, `app/_archived/`, `_archive/`, `app/work/WorkPage.tsx`+`work.module.css`, `RUN-STATUS*.md`) + the unused `motion` dep; **ESLint installed for real** (`.eslintrc.json` next/core-web-vitals — `npm run lint` no longer triggers first-run setup) + `npm run typecheck`; `.env.example` drops the phantom `ELECTRICITY_MAPS_API_KEY`, documents SITE_URL's new role.
  - Verified: `next build` clean (**26 routes** — robots/sitemap/icons now emit; sizes unchanged, GH 2.49kB); in-browser: head meta/OG/JSON-LD present, branded 404, `/api/ask` 503s generic, 4 pagers wrap correctly, MaterialRail drifts via DriftGroup, GH accent+serif+tabs+scheduler intact, home chip+taglines render, mobile 375px zero overflow, zero console errors. **Still open for Rishabh:** custom domain, real About portraits, LinkedIn link, CV refresh, OpenRouter spend cap, true participant count for the Follow claim.
- **WORLD-READY-2 (2026-07-03) — Rishabh's answers wired in.** Same day, closing four of the six opens:
  - **Follow research numbers restored (the real n, per Rishabh):** 5 concept tests (Henry, Michelle, Lara, Sharka + Prof. Alexandra Becker's Mar 31 session) + 2 expert interviews (Andy · agentic systems, Atanu Sinha · 25-yr operator) = 7. The page now reads consistently: timeline node "Peer tests · **4 peers**" + Alexandra's node relabeled "**concept test** · outside view · Mar 31" (4+1=5); research list matches ("Peer concept tests · 4 peers", "Alexandra Becker · Concept test · outside perspective"); prose "**Across five concept tests**, most people volunteered the same feeling…" and the quote credit "…the five concept tests kept surfacing the same feeling, unprompted." The lede's "six primary engagements" stays true (HW lived experience · workshop · peer tests · Andy · Alexandra · Atanu).
  - **Custom domain live:** `rishabhsalian.com` is connected on Railway (verified serving the portfolio). `lib/site.ts` fallback → the custom domain (SITE_URL env still wins if set), so canonicals/OG/sitemap/JSON-LD point at the real domain — the railway.app subdomain now canonicalizes to it.
  - **LinkedIn everywhere:** `https://www.linkedin.com/in/rishabh-salian117` — verified live via the browser (profile loads; it's the public URL LinkedIn itself lists). New `SOCIALS` in `lib/site.ts` = the single source; Footer (Email · LinkedIn · GitHub · Behance), the About contact row (LinkedIn button after Email), and JSON-LD `sameAs` all read from it. NOTE for Rishabh: the LinkedIn profile still says **Mumbai** as location + "Product & Strategic Designer" — update to Brooklyn/NYC to match the portfolio before recruiters cross-check.
  - **Model chain → best-intelligence free tier (per Rishabh):** `/api/ask` DEFAULT_MODELS now `nvidia/nemotron-3-ultra-550b-a55b:free` → `nvidia/nemotron-3-super-120b-a12b:free` → `openai/gpt-oss-120b:free` (the proven prod link) → `google/gemini-2.5-flash-lite` (paid net, unchanged). All four verified **tools-capable** against the live /models list (the MCP console requires it). If `OPENROUTER_MODEL` is set on Railway it still prepends — clear it if the new chain should lead.
  - Still open: About portraits (placeholders stay), CV refresh, OpenRouter dashboard spend cap.
- **SCROLL-REVEAL-1 (2026-07-06) — Housing Works' "appears on scroll" motion generalized site-wide.** Per Rishabh: HW was the only page whose section content faded/rose in on scroll; take that presentation everywhere.
  - **New shared `components/Reveal/` (generalized from HW's page-local `Motion.tsx` + `hw-motion.css`):** `Reveal.tsx` mounts once per page, marks `<html>.reveal-on` after hydration, and runs ONE `IntersectionObserver` (threshold 0.12, `rootMargin: 0 0 -8% 0`) over every `[data-reveal]`, adding `.is-revealed` as each enters view (then unobserves). `reveal.css` is the same feel HW used — `opacity:0; translateY(16px)` → `opacity:1`, 0.7s ease — as plain GLOBAL css (imported by the component; not module-hashed, since it's wired by attribute query). Progressive: no JS → `.reveal-on` never added → everything visible; explicit `prefers-reduced-motion` guard forces `[data-reveal]{opacity:1}` so content is never stuck hidden. An unused `--reveal-delay` hook is left in for optional per-item stagger.
  - **Applied by tagging section content wrappers with `data-reveal`** (mounted `<Reveal/>` after each page's `AmbientField`): Home 4 (arc · work grid · earlier · CTA), Follow 11 (every `.container`/`.containerText`), Greener Hours 9 (every section container), Healthy Materials 9 (`wrapWide` + field head + close). **Deliberately excluded:** every page hero (avoids a post-hydration hidden-flash on above-the-fold content — HW excludes its hero for the same reason) and the full-bleed interactive apps (FollowSandbox / GhApp / PackagesApp — no reveal transform on their ancestors).
  - **HW left untouched** — it keeps its own `.hw-js`/`.hw-reveal` system (plus its bespoke SVG line-draw + photo parallax, which the shared reveal doesn't cover). The two systems use disjoint markers (`.reveal-on`/`[data-reveal]` vs `.hw-js`/`.hw-reveal`) and coexist with zero conflict — verified live: HW still has 12 `.hw-reveal`, 0 `[data-reveal]`, no `.reveal-on`.
  - Verified: `tsc` + `next build` clean (26/26; sizes nudged up for the added component — home 9.13kB, HM 6.18kB, Follow 5.03kB, GH 2.6kB); lint only pre-existing `<img>` warnings. In-browser on all 4 targets: `.reveal-on` applied, correct `[data-reveal]` counts, observer fires on scroll + opacity transitions to 1, heroes stay visible, reduced-motion guard confirmed in the loaded CSS, zero console errors. **Note:** the Claude preview pane renders at ~682px wide regardless of the desktop resize preset (a pane constraint) — reveal logic is layout-agnostic so this doesn't affect verification, but real-viewport screenshots need a different tool.
- **COPY-A-1 (2026-07-08) — the site-wide copy pass from the full copy review (`Desktop\Claude\COPY-REVIEW-2026-07-07.md`).** Goal: simple, direct, human, student-voiced; no slogans, no jargon chains. Copy-only (no components/layout). Executed items 1–8 of the review's path-to-A:
  - **Home (resolves D-01):** kicker → "designer & builder"; h1 → "I design AI products — and build them, so you can try everything below."; footer CTA "Looking for a systems-minded designer?" → "I'm graduating Fall 2026 and looking for Design Engineer & Product roles."; new proof line under Selected work ("All four end in working software — open any card and try it.", `.workNote` in page.module.css); arc caption "AI-powered systems" → "AI-native products"; GH card tagline → "An open standard that puts a carbon label on AI compute."
  - **Follow:** hero h2 slogan ("AI made everyone faster. Follow makes teams smarter.") → problem fact ("The reasoning behind your team's work now lives in private AI chats."); tic pass (dropped "Not a link you send…" + "Less a search box…" constructions, dash-density trim in the sandbox walkthrough); CompareDiagram "with Follow" caption now glosses provenance ("who said what, where, when"); GraphView inline blurb de-duped from the rail blurb; prototype meta description split/shortened.
  - **Greener Hours (the jargon + voice page):** hero lede de-jargoned ("Every AI API response carries a new number — how clean the power behind it was. Three tools show what that unlocks…"), eyebrow "A design specification" → "A proposed standard", meta description matches; first person added in three spots (trade-off #03 "The sharpest critique I got at midterm…", §06 "I built the project against that framework", KPI caveat "the moves I'd watch for"); "legible" reduced to the closing line only; "§ 08 · If the wedge works" → "If it works"; en-US "data-centre" fixed in ScaleChart + VisibilityFlow; chat model chip "Sonnet 4.6" → "4.7" (matches DashboardMock's table); "cleanest hour" → "cleanest window" (T2 blurb + chat context string).
  - **Healthy Materials:** byline "Research capstone" → "Graduate capstone" (framings 5→2); stage labels "the turn · areas of opportunity" ×3 → "the turn" (the lede's "five leverage points" keeps the term); research lede de-templated from Follow's ("Mixed methods on a deliberately small scope:…"); prose "business-as-usual" → "business as usual"; dropped "over a real model API" from §9 (the honesty strip carries it). HM keeps the site's single "a working slice" (its own page + prototype meta).
  - **Housing Works:** proposal card "Trustee-Led Workshops" → "Trustee Workshops" (naming now 2 forms: product name + lowercase descriptive); attribution "mine" → "me"; the acknowledged-over-pay quote now appears once (Finding 02; Workshop-02 body paraphrases); "Bridges and Barriers" → "Bridges & Barriers" (matches timeline/stat strip); Scene 9 "a working slice" → "the working version" + new "$900 of the $2,700" budget-share sentence; prototype kicker/meta → "illustrative data" / "working version".
  - **Honesty-strip alignment:** GhApp footer now follows the canonical skeleton the other three products use — "Concept prototype · illustrative data — {specifics} · {live-API note}".
  - docs/DECISIONS.md D-01 marked RESOLVED (alternate h1 candidates logged in the review doc).
- **ROUND-2026-07-08 (shipped 2026-07-09) — Rishabh's 17-item PDF notes, all four surfaces (plan: `~/.claude/plans/jaunty-moseying-thunder.md`; 3 Sonnet agents + Fable on GH).** His calls, asked upfront: HW recording = illustrative player (no audio) · remove only the ribbon flourishes · T2 sim clock stays, reframed as a demo clock.
  - **GH (`ec8c2a7`):** GhSim on monotonic `simT` (hour derived; `GRID2` day-2 curve + `gridAt(t)`; `findCleanest` absolute over 48h; jobs store absolute times; `submitJob(name, deadlineT, targetT?)`). T2: day+time deadline pickers (Today/Tomorrow + hour, past hours disabled), 48h clickable forecast (tap a bar within deadline = user-picked window; midnight divider + deadline marker), demo-clock strip demoted under the form ("fast-forwards the day…", Pause/＋1h — pause still freezes T1/T3; verified live 462g→452g in lockstep on +1h). T3: GREEN WINDOW USE block (Daily/Weekly/Monthly `aria-pressed` toggle over deterministic datasets 46/42/38% green; live "{flex.count} jobs routed" line; used-vs-potential bars labeled "sim · illustrative"). P3 insight expanded to two sentences. Old dark-clock CSS deleted.
  - **HM (`f7fe122`):** PackagesApp opens on intake (`intakeOpen` init true; new "or skip to a pre-built package ▸" escape via `onSkip` prop; **intake focus got `preventScroll` — without it page-load scroll-jacked to §9**). MaterialRail swipes <768px (swipe-row recipe; `transform:none !important` on cards cancels DriftGroup below the breakpoint). Affordable-housing framing moved from the frame intro to a new origin paragraph before the three concepts ("Walk through NYC rentals and the interiors repeat…"). Journey lede names the journey map as the framework.
  - **HW (`9159612`):** shared `.iconCard` treatment across How-we-worked (4 new hw-line glyphs: documents/storefront/people/poster; 2×2 ≥768px), What-we-found, What-we-proposed. `selNeedId` starts null (StepTrack shows 01). Archive: `HwArchived` + `duration`/`insight`/`transcript?`, `HwCapture` + `insight`; 4 seeds got 5-turn transcripts + insights, 6 captures got insights, `archiveSession` copies all three; entries expand (button header `aria-expanded`, one-at-a-time) into deterministic 40-bar waveform + "illustrative recording · stand-in — no audio" + transcript + highlighted insight. Both ribbons + their CSS removed (hw-draw glyph system intact).
  - **Follow (`c1ab34b`):** heroTitle "A shared memory for your team's AI chats." + simplified sub; problem statement promoted to `.probState` display line inside The problem; new `TermTip.tsx` (GhApp tooltip mechanics + measured 360px viewport clamp via `--tt-shift`; `<span role="tooltip">` not div — phrasing-content/hydration) replaces the defNote aside; CompareDiagram circles say "AI" with "Maya's/Alex's/Sam's" sub-labels (in-circle placement forced by connector-line geometry, `getBBox`-verified), PILLARS → "one memory · sources kept · disagreements flagged · who knows what", both arias rewritten; reel captions rewritten plain (role names kept).
  - **Gates:** `tsc` + `next build` clean (26/26, sizes flat); fetch-text assertions green on all 4 pages; GH interaction battery live (queue 2→3→4 incl. tap-to-pick landing on the tapped hour; period toggle; shared-clock lockstep); HW archive expand verified functionally. **Gotchas:** agents sharing ONE preview tab clobber each other's navigation (same class as the `.next` gotcha — serialize or use separate servers next time); backgrounded preview tab reports `innerWidth: 0` → all layout math bogus + screenshot tool times out (functional/ARIA checks still fine).
  - **Known cosmetic leftovers (flagged, not fixed):** HM `#sheet` deep-link on cold load shows process step 01 under the open sheet (intake now starts open); Follow reel caption pill can overlap the corner mark <~380px (pre-existing, spawn-task chip filed).
- **FOLLOW-SYS-1 (2026-07-10) — the two post-HMW diagrams became one interactive system picture; what-it-does strip; sandbox onboarding tour.** Per Rishabh: combine the "With Follow" concept diagram + the "How it works" pipeline reel into ONE borderless diagram floating over the ambient (glass, no frame), hover explains each part in plain terms (the technical architecture framing dropped); the reclaimed space shows what Follow does / how it differs from RAG / product features; the demo keeps its story and gains an onboarding-style walkthrough.
  - **New `app/work/follow/FollowSystem.tsx` + `.css` (client, namespaced `.fsys`):** three person·tool glass chips (Maya·Claude / Alex·ChatGPT / Sam·Gemini — the sandbox avatar colors), dashed wires with write packets (accent orange, down) + read packets (Persian blue `#1C39BB`, up), two floating flow pills, and a glass "The Follow index." slab (pillars line kept). Four stations — team · capture · layer · recall — are real buttons (TermTip's proven onMouseEnter/Leave + onFocus/Blur quartet; hover state separate from focus/tap state); active station lifts, the rest dim to 0.42, opposite-direction packets hide; a persistent `role="status"` caption dock below swaps tag + plain-language body + a canon example (guest-default decision, Stripe-vs-Adyen contested pair, payment-sheet spec — the demo's own data). Idle bob + packet motion gated on `prefers-reduced-motion: no-preference`.
  - **Page restructure (`page.tsx`):** response section = reveal + FollowSystem (framed `CompareDiagram variant="after"` + its note gone); the whole "How it works" reel section + the stray pipeline note section DELETED (`FollowReel.tsx/.css` removed); new "What it does" section = RAG-vs-Follow compare strip (`.rag*`, muted "Finds text that matches." vs accent-tinted "Finds the source.", floating `vs` chip, Glean footnote) + six feature tiles reusing `.cards/.card` (works across tools/MCP · receipts · disagreements visible · who-knows-what · supersession in plain words · files join); the after-sandbox "What it accomplishes" section deleted (absorbed into the tiles). CompareDiagram simplified to before-only (variant prop + after markup/CSS/packet keyframes pruned; ~90 dead CSS lines).
  - **Demo onboarding (`DemoTour.tsx`):** the dense sandbox how-to paragraph became a 5-stop numbered tour (skim week · open a conversation · spin the graph · ask Follow · watch the wire) — glass step cards, swipe-row <1000px, each "show me ↓" dispatches `follow:goto` + scrolls to `#follow-sandbox` (`scroll-margin-top` clears the nav). FollowSandbox listens: plain views via the existing `goView`; `"ask"` opens the Ask overlay only in the ≤719px shell (desktop dock is always visible). Story card kept as-is.
  - **Gates:** `tsc` + `next build` clean (26/26; follow 5.29 kB); live battery — all 4 stations swap the dock + dim + hide counter-flow packets (idle restores), all 5 tour stops switch the sandbox (incl. mobile ask overlay open/close), RAG strip 2-col/1-col, tour rail grid→swipe, 375px zero h-overflow; headless-Chrome screenshots for visual proof (preview tab backgrounded → screenshot tool times out, the known gotcha). **Verify-in-bg-tab notes:** synthetic mouseover/pointerover can NOT drive React enter/leave (the proven TermTip control fails identically — not a code signal); React 18 flushes after synthetic `focusin`/clicks only on a later macrotask — use unthrottled MessageChannel ticks, never `setTimeout` (background throttling stretched a 30s eval past timeout).
  - **Parallel-session note:** built alongside the SectionHeader session (`17c66c3`, which deferred Follow on purpose); zero file overlap. Follow-up: roll `components/SectionHeader` onto the restructured Follow page in a later pass.
  - **Round 2 (same day) — the index opens.** The slab is Follow-COLORED now (accent tint→wash gradient + soft orange glow; backdrop-filter gone) with a persistent-but-faint hover-brightened prompt chip ("click to look inside ▾" / "close ▴"). Click (aria-expanded, Esc closes) unfolds an inside view from the slab's top edge (grid-template-rows 0fr→1fr; viewport = navy #0a1f33, the reel's dark world): a 12s CSS loop where a fact drops in from a chat, pops into an existing 7-node graph, links to two related facts, clashes with Alex's read (dashed red tie + pulses on both parties, both kept), then a query ring fires and a blue packet exits with the source — five mono captions track the beats. Animations scoped to `.fsysOpen` (restart on every open) + gated on no-preference; reduced motion gets the finished static graph + one static caption. Layer/idle dock captions now prompt the click. Verified live: toggle/ARIA/graph elements/animation attach-detach, open=312px ↔ closed=0 (transition-off probe — parked transitions in a backgrounded tab read start values).
  - **Round 3 (same day) — the inside view speaks the real graph's language.** The mini-graph now mirrors `lib/followGraph.ts`'s actual ontology (nodes: member ◉ / conversation ▢ / file ▤ / fact ● / topic ◌; edges: produced · authored · uploaded · topic-tag · contradicts — the five types) in GraphView's color language (facts accent `#e8703a`, threads/members tool-colored, files gold, topics neutral), with a **legend footer strip** under the viewport and labels drawn from the sandbox's own facts ("41% drop at shipping reveal", "SCA: no retry code", "Alex's LTV read", threads + the two uploaded files). Loop extended to 16s with a **versioning beat**: payment-sheet spec v2 falls in, a white supersedes-arrow draws to v1, and v1 ghosts (dim + dashed ring + "superseded · kept" legend entry) before everything resets. 6 caption beats; dotted/dashed edges reveal by opacity (dash-draw would just slide the pattern). Node labels hide <620px (captions + legend carry it). Verified live: full census (3/4/2/1 + 9 facts, 6+9+2 typed edges, tie/sup/tip/ghost, 19 labels, 10 legend items, 6 captions), all 15 animation tracks attach at 16s only while open, label-collision scan clean after two anchor fixes. **Gotcha: the user was interacting with the open slab during verification — toggling it mid-eval inverted my reads; re-verified by reading state first and leaving it untouched.**
  - **Round 4 (same day) — the demo tour became a SPOTLIGHT tour.** DemoTour's "show me" now runs onboarding-style: the page dims behind FOUR fixed panels (portaled to body, z-1180) leaving a live hole around the target — `[data-tour="main"]` (the sandbox's active-view column, new attr) or `[data-tour="ask"]` (the Ask dock root, new attr in FollowAskDock) — with an accent ring and a bottom-center glass coach card (stop n/5 · title · line · ← back / next → / finish ✓ / ✕, Esc or dim-click ends, focus handed to the card + restored on close). The hole is real (panels, not a box-shadow cutout) so the spotlighted region STAYS INTERACTIVE — verified elementFromPoint inside the hole hits sandbox content. A rAF loop re-measures the target every frame (tracks scroll/resize/view swaps; document.hidden-guarded) plus one synchronous measure in goto() so the first paint is placed even where rAF is parked. ≤719px the ask stop falls back to spotlighting the whole sandbox frame (the dock is an overlay there). Verified live at 477px + 1366px: all 5 stops step (views switch under the hole), ring↔target alignment on main AND the desktop dock, back/finish/Esc/dim-click all close. **Gotcha: editing a client component under Fast Refresh by INSERTING a hook shifts hook slots → React spams "final argument changed size between renders" with huge stacks (74KB) until a hard reload — the warning is the HMR artifact, not the code; always hard-reload after hook-order edits before judging console output.**
  - **Round 5 (same day) — tour cards fully clickable · landing flash · auto-start with skip.** (1) Each tour card is clickable edge to edge via the stretched-hit pattern (`.tourGo::after { inset: 0 }` over the `position:relative` card; hover-lifts like `.card`; button aria-label carries the stop title; `@supports selector(:has(*))` moves the focus ring onto the card). (2) The spotlight ring is keyed per stop and plays a `spotFlash` double-pulse (box-shadow spread 14px→4px→9px→4px, 0.75s, no-preference gated) every time it lands — "flashes to show where it is". (3) The tour AUTO-STARTS once per mount at stop 1 when the sandbox scrolls ~35% into view (IntersectionObserver on `#follow-sandbox`, disconnects after fire or any manual start) — no scroll-jack (`goto(0, {scroll:false})`), no focus steal (focus only moves to the coach card on manual starts); stop 1's left button is **"skip tour"** (later stops get "← back"; ✕/Esc/dim-click still end it). Verified live in a foregrounded tab: auto-open at stop 1 with skip + no focus steal, skip/Esc end, auto never re-fires after dismissal, card-center elementFromPoint hits the stretched button and opens its stop with focus + back, flash animation attached. Console: only the stale pre-reload hook-shift warnings (buffer is cumulative across reloads — diff dumps or check the entry count before blaming new code).
  - **Round 6 (same day) — clicking into the spotlight hands over control.** A capture-phase document `click` listener (active only while the tour runs) ends the tour whenever the click target is inside `#follow-sandbox` — no preventDefault, so the click lands normally and the sandbox just works ("click" not pointerdown, so touch scrolls inside the hole don't dismiss). Coach card gained the teaching line "or just click in — the tour gets out of the way" (`.spotHint`). The card/dim panels live on document.body so they never match the closest() check; dims keep their own click-to-end. Verified live post-reload: clicking a feed row inside the hole kills ring+dims AND the click lands (sandbox intact), the sandbox then drives normally (Files view switch), manual tour restart + Esc exit still clean.
- **FOLLOW-SYS-2 (2026-07-11) — the tour confined to the sandbox; the before-diagram joins the design language.** Per Rishabh ("the demo ui and guide needs to stay in the sandbox… follow the same design language for the before follow diagram"):
  - **Tour confinement:** the spotlight overlay no longer rides the viewport — it portals into `#follow-sandbox-stage` (the `appBleedInner`, now `position:relative`) and every piece is ABSOLUTE within it (`.spot` z-140; dims/ring stage-relative, measured as target-rect minus stage-rect), so the whole tour scrolls WITH the sandbox: scroll away and it stays behind in the demo. The coach card sits in a **sticky dock** (`.spotDockWrap` absolute inset-0 flex-end + `.spotCard { position:sticky; bottom:14px }`) — pinned to the viewport bottom while the sandbox is on screen, anchored to the sandbox's bottom edge once you scroll past. The click-inside-dismiss listener now excludes the overlay's own controls via `[data-tour-overlay]` (they're inside `#follow-sandbox` post-portal — without the exclusion, "next →" would end the tour). Auto-start unchanged (fires when the sandbox hits 35%, contained to the sandbox now).
  - **`BeforeSystem.tsx`** (server, shares FollowSystem.css as `.fsys.fsysBefore`) replaced the framed `CompareDiagram` (both files DELETED; only comment references remain in ProjectThumbs): same chips/wires/slab vocabulary but grey and broken — slate dashed wires, orange packets that **evaporate mid-wire** (`fsysLost`), a static "✕ no shared memory" pill, and a navy "The meeting." slab (partial recaps · no sources · forgets what it doesn't know) that deliberately rhymes with the orange Follow-index slab. reelNote → "Adapted from the capstone deck…".
  - Verified live: before-diagram census (3 chips/wires/lost-packets + pill + meeting slab + caption, no `.cdHolder` anywhere), overlay parent = stage + absolute/sticky computed, ring↔main aligned, **scroll-to-top leaves the tour behind with the sandbox** (ring document-position unchanged, out of viewport), "next →" survives the click-inside rule, sandbox clicks still dismiss + land, Esc clean. `tsc` clean. Auto-start unverifiable this pass (tab fully backgrounded: `document.hidden`, innerWidth 0, control IO never delivers — code path unchanged from the round-5 foreground verification).
- **FOLLOW-SYS-3 (2026-07-11) — documents join the diagrams.** Per Rishabh ("follow also supports documents, live documents like the one in GWS and the docs people chose to upload… incorporate it into the graphs instead of just chats"; correction: docs are THE SOURCE, never "stale" — "Follow follows the artifact"). Proposal mocked via show_widget first, then built:
  - **FollowSystem:** the top row is a 4-column sources grid — the three person·tool pairs (spanning cols 1–3) + a "Team docs · live · uploaded" chip (col 4, nudged 14px lower: a source, not a teammate; gold 2-bar doc avatar on `--gold`). Fourth wire lane at 87.5% (gold-tinted dashes) carrying SQUARE write packets (shape grammar: dots = chats, squares = docs); pills recentered on 37.5/62.5%. Docs is a fifth station — caption leads with "Follow follows the artifact — a chat or a document, same treatment", live-doc re-sync + uploads explained, sandbox eg (usability-notes.pdf, the spec versioning up). Emphasis rules: docs active hides chat traffic and brightens its lane; capture/recall hide the doc squares.
  - **Inside view:** the payment-sheet spec moved from a conversation node to a LIVE FILE node ("payment-sheet spec — live", 3 files/3 convs now); the versioning beat is the shipped product's re-sync supersession: a gold dashed sync pulse fires on the doc (40–46%), an orange square packet rides the produced-edge path to fact v2 (45–53%), v2 pops → supersedes → v1 ghosts (55–66%); query beat shifted to 68–77%; caption 4 = "the spec doc re-syncs — v2 supersedes v1; the old version stays in the trail"; cap6 "one graph — chats and documents, every version kept"; legend file item → "file — uploaded · live"; v1/v2 labels renamed "spec fact · v1/v2" (fact vs doc). Old second falling packet (fsysGPktIn2) deleted.
  - **BeforeSystem:** same 4th lane — the docs chip IDENTICAL to the after-world's ("live · uploaded"; the source is fine, the sharing is broken) with its square packets evaporating mid-wire (fsysLostSq); figcaption notes the docs sitting off to the side.
  - **Copy:** tile 06 → "Documents join the memory" + "Follow follows the artifact"; recall caption "in which chat or doc"; idle mentions the documents. Hero left chat-first per my recommendation (unobjected).
  - Verified live: 4-col row + chip-over-lane, docs station dock/dim/traffic rules, inside census (3 files, sync+doc-packet anims attached, old packet gone, labels/legend/captions), before-diagram squares + zero "stale" text, label-collision scan clean, tsc clean, no console errors. Mobile: `.fsysDocsSub` hidden ≤620px.
  - **Round 2 (same day) — one-liner tour pills + RAG-card glyphs.** Per Rishabh ("taking too much space, make these 1-liner cards and let the people see for themselves… icons for finds-text and finds-source, portfolio style"): the five tour cards collapsed into glass PILL buttons (number · title · ↓, flex-wrap rail, hover-lift; li display:contents; the stretched-hit/`:has` machinery deleted — the pill IS the button) — each stop's descriptive line now lives only in the spotlight coach card. The RAG compare cards gained portfolio-style line glyphs (sandbox icon recipe: currentColor stroke 1.7 round): the SAME magnifier on both — over text lines (typical RAG, soft grey) vs over a mini graph (Follow, accent) — the parallelism is the argument. Verified live: 5 pills wrap cleanly (no description lines in the rail), pill click still opens the spotlight with title+line in the coach card, icon strokes/shape counts correct, console clean.
- **UNFOLD-POLISH (2026-07-16, branch `ship-interactions` — revises the unmerged ROUND-2026-07-16 Unfold work; that base round's changelog entry still lands with Rishabh's merge).** His live-review feedback: uneven collapsed cards, glitchy/unintuitive "+ more" disclosure, robotic mono chrome, motion not gentle enough. Four commits:
  - **Unfold rebuilt** (`3bdde7a`): affordance = ONE page-accent chevron (rotates 180° open, nudges on hover; `--unfold-affordance` host override → HW's dark workshop glass uses the wash); hover-peek REMOVED — hover never changes layout anywhere on the site now; open/close = measured-height accordion (CSS owns the settled 0/auto states, the component pins inline px between them; replaces the fragile `grid-template-rows 0fr→1fr` trick). New gentle tokens: `--t-gentle .45s`, `--t-soft .35s`, `--e-gentle cubic-bezier(.22, 1, .36, 1)`.
  - **Collapsed evenness** (`e0f3194`): each host grid reserves its tallest measured header via `--unfold-head-min` (Follow .cards 140px 760–999 / 112px 1000–1199; GH .tradeoffs 120px 760–999; HM .concepts 176px ≥768 / 144px swipe row; HW .findings 186px 768–1079 / 170px ≥1080; other ranges naturally even). Values are live-measured — re-measure if titles change.
  - **Gentle-hover pass** (`4e35c0e`): every card surface lifts at most -2px on `--t-soft --e-gentle` (.lift, ProjectCard, ArchiveCard, EarlierWorkDeck, About portrait tiles, Follow .ins/.tourGo, GH .force/.prec, HW .sitePhoto — its bespoke curve became the shared token); transforms gated behind no-preference. Micro-controls (press dips, nav links, side-nav dot) stay fast by design.
  - **Label-face trial** (`9cd4427`): `--font-label` replaced mono across ALL portfolio chrome — 104 refs / 21 files, including the global `.mono` utility (every call site is narrative chrome; candidate follow-up: rename it `.label`). The six demo replicas deliberately keep IBM Plex Mono (WorkshopsApp's recording label pinned explicitly). Candidates: A Bricolage caps (default) / B Fraunces caps (trial-only global load in layout.tsx) / C Inter caps — cycled live by the dev-only bottom-left LabelFaceSwitcher chip (localStorage-persisted). **PENDING: Rishabh picks → lock the winner in globals, delete the switcher + trial scopes + trial Fraunces load.**
  - Verified: typecheck + build clean at every commit (26 routes); open/close end-states + the transitionend→auto handoff (hidden-tab transition-off probes per the playbook); collapsed evenness at 375/768/1000/1080/1280; all four accent chevrons native + HW wash contrast; demos still mono (sandbox url/meta/pill probed); hw-js + 13 hw-draw glyphs intact + finding Unfolds toggle; zero horizontal overflow at 375; zero console errors across all five pages.
  - **Glass round (same day, `UNFOLD-POLISH-6`):** every dropdown now sits in a glass window — Unfold's chrome became the site glass recipe (`--glass-bg` + `--glass-blur` + `--line` hairline; ambient blurs through) with `--unfold-glass-bg/-line` host hooks; the `bare` variant was RETIRED (one look site-wide — all former bare call sites are glass boxes; HW's workshop-01 Unfold inside the dark `.imgGlass` retints to a faint cream veil `rgba(244,242,236,.07)`). Follow's research columns became glass Unfolds with counts ("Secondary · the theoretical spine — 6" / "Primary · engagements — 6", label shortened from "six engagements" to dodge the count redundancy), mirroring HM's research pattern; `.rcol` wrappers retired, `.rcols` gained `align-items: start`. UnderTheHood's tech notes got the same glass window (`.hoodGlass` inside the clip) + its chevron/panel retimed to the gentle tier. Verified: glass computed on all four pages (+ the dark veil), research toggle 0→186→0, Packages defaultOpen intact, concepts still 176-even, 375 research swipe row 272px snap cards, zero overflow, zero console errors, build 26/26.
