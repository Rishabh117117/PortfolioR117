# Open Decisions
- D-01 The one impression: hire-me designer / fund-Follow founder / systems-thinker hybrid. — OPEN
- D-02 Follow demo: self-contained sandbox vs wired to real Follow MCP backend. — RESOLVED (FOLLOW-PAGE-1): self-contained. The on-page artifact is an **illustrative animation** of the pipeline (autoplay-on-view since RESTRUCTURE-1; not a live demo, not wired to a backend). A "team-memory sandbox" CTA is present but its route isn't built yet (placeholder `href="#"`).
- D-03 Which prototypes are genuinely working vs concept-stage (sets WORKING/SIMULATED badges). — RESOLVED for Follow (FOLLOW-PAGE-1): **SIMULATED** (default). The page's `DemoCallout`, `lib/projects.ts` Follow `status`, and the home-page Follow `DemoCallout` are all SIMULATED and kept in sync. **Override pending Rishabh's confirm:** present Follow as WORKING (the underlying product is real/deployed) — if so, flip all three together. Still OPEN for the other flagships.
- D-04 About-me content: bio, photo, role history. — OPEN
- D-05 Current CV file. — OPEN
- D-06 Greener Hours case study: bespoke React page vs the finished presentation deck. — **REVERSED → RESOLVED (GH-PAGE-1, 2026-06-30): build the bespoke narrative page.** GH-DECK-1 first shipped the deck embedded in a 16:9 iframe; per Rishabh, `/work/greener-hours` was then rebuilt as a full narrative case study "like Follow / HW / Healthy Materials, using a lot of elements from the deck." HYBRID treatment: portfolio shell + Bricolage, **scoped Fraunces** (`--font-serif`, loaded in the page only) for display, the deck's **amber/navy as page-scoped vars** carried into faithfully-rebuilt SVG diagrams (scale chart, dark-matter flow, three forces, three surfaces, Tier-1 chat mock, API-headers, HTTPS curve). CURATED product depth: three-surface overview + one full Tier-1 mockup. Primary accent stays **Forest `#1C3B36`** (matches the card; the amber lives only in the diagrams/atmosphere). The **original deck stays vendored** (`public/greener-hours/index.html`) and is linked "View the full deck ↗". Badge `COURSE PROJECT · CONCEPT`; demo stays **SIMULATED** until the interactive Tier-1 prototype is wired through `app/api/ask` (deferred, Phase 4 → would flip to `LIVE API`). The prior deck-as-page rationale (self-scaling artifact, no re-implementation) was sound but is superseded by the user's preference for a portfolio-native narrative.

## Log
- FOLLOW-PAGE-1 (2026-06-19): bespoke `/work/follow` built (scroll-pinned pipeline reel + deck timeline + research). Follow accent overridden Persian Blue `#1C39BB` → burnt orange `#C2410C` across page, home grid card, and /work card. Resolves D-02; resolves D-03 for Follow (SIMULATED, pending the WORKING override).
- RESTRUCTURE-1 (2026-06-25): reel section moved from full-viewport scroll-pin to inline two-up (2/3 reel autoplay-loop + 1/3 write-up). Write-up copy provisional.
- RESTRUCTURE-1 follow-up (2026-06-26): removed the dark stat strip (5 LLM roles · 3 tensors · 5 edge types · 12 MCP tools) from `/work/follow`, per request.
- DESIGN-PHASE-1 (2026-06-27): `/work/follow` design-phase pass. (An animated before/after morph reel was prototyped + reviewed but **rejected by the user** in favour of two static diagrams — never committed.) Shipped to the working tree:
  - **Two static deck diagrams** (`CompareDiagram.tsx`/`.css`, server component, `variant="before"|"after"`) reproducing the deck's slide-13 compare halves (deck saved to `docs/prototypes/follow-capstone-deck-v4.html`). Diagram 1 (Before) sits after *The problem*; Diagram 2 (With Follow) after the insights. Only motion is the subtle read/write packets in the With-Follow variant (reduced-motion off).
  - **IA restructure:** research methodology (spine + 6 engagements) **clubbed under the timeline** (the journey); a new **"Insights & areas of opportunity"** section (the 3 deck insights + guilt quote) sits between the two diagrams. Narrative: hero → journey+research → problem → Before diagram → insights → With-Follow diagram → how-it-works pipeline reel (kept) → differentiators → four questions → where-it-sits → demo.
  - **Bolder hero:** giant `Follow.` display lockup (Bricolage 800, clamp 76–156px, orange period) as the h1, above the existing headline/sub.
  - **Motion phase begun** (was deferred in DESIGN.md §7/§10): an `AmbientField` backdrop (soft drifting orbs, pointer + scroll parallax, reduced-motion-still) plus a glass hover-lift on the differentiator/insight cards. Subtle/clean per the user; reuses locked tokens + Follow burnt-orange; no new deps (`motion@12` still unused).
- DESIGN-PHASE-1 v3 (2026-06-27): per user feedback — (1) the ambient promoted from hero-scoped to a **page-wide `position:fixed` backdrop** (z-index 0) with all content on a `.pageContent` z-index:1 layer, so the soft background runs through the whole page (Material-style layering); (2) the two compare diagrams **refined** — glass-elevated panels, the Follow Index now names its 4 pillars, the team is named (Maya/Alex/Sam), consistent strokes/spacing; (3) a **transactive-memory-system definition aside** (`.defNote`, textbook-style) added to the problem section; (4) the **"four questions" section removed** (+ dead `.queries/.q` CSS; DemoCallout copy reworded off it). `next build` clean; verified live.
- DESIGN-PHASE-1 v4 (2026-06-27): narrative + motion overhaul, per user. New flow: **hero → timeline+research → ONE white block (`.blockWhite`) holding {problem+TMS · the BEFORE diagram · insights} → HMW statement → Follow concept reveal (big `Follow.` + the WITH-FOLLOW diagram, with the old "where it sits"/Glean/MCP copy folded into its note) → how it works (pipeline reel) → demo → "what it accomplishes" (the old differentiators, reworded)**. Diagrams **reordered** to `web→AI→user→meeting` (before) and `web→AI→Follow Index→user` (after) — meeting last; variant-specific layouts. **Ambient** now drifts **side-to-side** and carries **blue (`#1C39BB`) + orange** orbs whose opacity cross-fades by scroll (intro=both → problem=orange → Follow=blue) for a cleaner colour arc. **Pipeline reel** base frame fixed to start **blank** (`.ed { opacity:0 }` until `.fp3`; was leaking `.ed.con` + the `.ed.sps` arrow marker). `next build` clean (4.72kB); flow + diagram order + reel base + console all verified live.
- GH-DECK-1 (2026-06-30): bespoke `/work/greener-hours` built as a **deck-as-page** (resolves D-06). The finished presentation deck was vendored verbatim to `public/greener-hours/index.html` (Next serves it at `/greener-hours/index.html`) and embedded in a 16:9 iframe (`aspect-ratio:16/9`, height-capped to the viewport) so the self-scaling 1920×1080 canvas fills the frame with no letterbox; a mono "Open full screen ↗" link opens the raw deck. The page reuses the `[slug]` project pager (← Follow / Healthy Materials →) and sets `--accent` from `lib/projects.ts`. `"greener-hours"` added to `BESPOKE_SLUGS` so the dynamic route no longer emits it (no duplicate). The deck is a **vendored asset** — its markup/CSS/JS, fonts, and amber palette are untouched; no design tokens applied to it. `lib/projects.ts` left as-is (card accent Forest `#1C3B36`, status `SIMULATED`). The interactive prototype embed is the deferred follow-up.
- GH-PAGE-1 (2026-06-30): **retired the iframe deck-as-page; rebuilt `/work/greener-hours` as a bespoke narrative case study** (reverses D-06). Hybrid look (Bricolage + scoped Fraunces display + the deck's amber/navy in the diagrams; Forest primary accent), CURATED product (3-surface overview + full Tier-1 chat mockup). New files: `lib/greenerHours.ts`; `app/work/greener-hours/{AmbientField, ScaleChart, VisibilityFlow, ForceVisual, HeadersDiagram, AdoptionCurve, SurfaceThumbs, ComputeWindowMock}` + `diagrams.css`; rewrote `page.tsx` + `greener-hours.module.css`. `lib/projects.ts` status `SIMULATED` → `COURSE PROJECT · CONCEPT` (Forest accent unchanged). The vendored deck is kept + linked ("View the full deck ↗"). `next build` clean (20/20; `/work/greener-hours` static 3.5 kB); verified in-browser (Fraunces scoped, amber/navy/forest semantics, 3 dark bands, no h-overflow desktop+mobile, no console errors).
- GH-PAGE-1 restructure (2026-06-30, same day): per Rishabh — reordered so the HTTPS precedent + 3 supporting precedents sit **right after the reframe** (one combined dark "the reframe, proven" section), then opportunity → product; the cramped reframe quote restyled to a clean full-width opener; and the product became the **three surfaces in a tabbed view** (`TierTabs`) with all three fully rebuilt (`ComputeWindowMock` T1, `SchedulerMock` T2, `DashboardMock` T3 — superseding the curated one-full-two-thumbnails approach; `SurfaceThumbs` removed). Build clean (8.54 kB); DOM-verified + 3-lens review fixes applied.
