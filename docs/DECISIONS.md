# Open Decisions
- D-01 The one impression: hire-me designer / fund-Follow founder / systems-thinker hybrid. — OPEN
- D-02 Follow demo: self-contained sandbox vs wired to real Follow MCP backend. — RESOLVED (FOLLOW-PAGE-1): self-contained. The on-page artifact is an **illustrative animation** of the pipeline (autoplay-on-view since RESTRUCTURE-1; not a live demo, not wired to a backend). A "team-memory sandbox" CTA is present but its route isn't built yet (placeholder `href="#"`).
- D-03 Which prototypes are genuinely working vs concept-stage (sets WORKING/SIMULATED badges). — RESOLVED for Follow (FOLLOW-PAGE-1): **SIMULATED** (default). The page's `DemoCallout`, `lib/projects.ts` Follow `status`, and the home-page Follow `DemoCallout` are all SIMULATED and kept in sync. **Override pending Rishabh's confirm:** present Follow as WORKING (the underlying product is real/deployed) — if so, flip all three together. Still OPEN for the other flagships.
- D-04 About-me content: bio, photo, role history. — **RESOLVED (ABOUT-PAGE-1, 2026-07-01):** Rishabh supplied the approved sandbox build (`about me page.html`) — bio/thesis, journey timeline (2016→now, two pivots), role history (matches the CV), skills, contact handles (GitHub `Rishabh117117`, Behance `rishabhsalian`). **Photos stay designed placeholders** (portrait/studio/Mumbai) until real images land.
- D-05 Current CV file. — **RESOLVED (CV-DOWNLOAD-1, 2026-07-02):** the dedicated `/cv` route is REMOVED. The CV is a **direct PDF download** from the About page's contact block (`public/Rishabh-Salian-CV.pdf`, the supplied file — Rishabh accepts it despite being a little dated). Nav dropped the CV link (Work · About · Contact); the home CTA "View CV" → "About & CV" now points at `/about`. Replace the PDF in `public/` when a fresher CV exists — no code change needed.
- D-06 Greener Hours case study: bespoke React page vs the finished presentation deck. — **REVERSED → RESOLVED (GH-PAGE-1, 2026-06-30): build the bespoke narrative page.** GH-DECK-1 first shipped the deck embedded in a 16:9 iframe; per Rishabh, `/work/greener-hours` was then rebuilt as a full narrative case study "like Follow / HW / Healthy Materials, using a lot of elements from the deck." HYBRID treatment: portfolio shell + Bricolage, **scoped Fraunces** (`--font-serif`, loaded in the page only) for display, the deck's **amber/navy as page-scoped vars** carried into faithfully-rebuilt SVG diagrams (scale chart, dark-matter flow, three forces, three surfaces, Tier-1 chat mock, API-headers, HTTPS curve). CURATED product depth: three-surface overview + one full Tier-1 mockup. Primary accent stays **Forest `#1C3B36`** (matches the card; the amber lives only in the diagrams/atmosphere). The **original deck stays vendored** (`public/greener-hours/index.html`) and is linked "View the full deck ↗". Badge `COURSE PROJECT · CONCEPT`; demo stays **SIMULATED** until the interactive Tier-1 prototype is wired through `app/api/ask` (deferred, Phase 4 → would flip to `LIVE API`). The prior deck-as-page rationale (self-scaling artifact, no re-implementation) was sound but is superseded by the user's preference for a portfolio-native narrative.

## Log
- FOLLOW-PAGE-1 (2026-06-19): bespoke `/work/follow` built (scroll-pinned pipeline reel + deck timeline + research). Follow accent overridden Persian Blue `#1C39BB` → burnt orange `#C2410C` across page, home grid card, and /work card. Resolves D-02; resolves D-03 for Follow (SIMULATED, pending the WORKING override).
- RESTRUCTURE-1 (2026-06-25): reel section moved from full-viewport scroll-pin to inline two-up (2/3 reel autoplay-loop + 1/3 write-up). Write-up copy provisional.
- RESTRUCTURE-1 follow-up (2026-06-26): removed the dark stat strip (5 LLM roles · 3 tensors · 5 edge types · 12 MCP tools) from `/work/follow`, per request.
- DESIGN-PHASE-1 (2026-06-27): `/work/follow` design-phase pass. (An animated before/after morph reel was prototyped + reviewed but **rejected by the user** in favour of two static diagrams — never committed.) Shipped to the working tree:
  - **Two static deck diagrams** (`CompareDiagram.tsx`/`.css`, server component, `variant="before"|"after"`) reproducing the deck's slide-13 compare halves (deck saved to `docs/prototypes/follow-capstone-deck-v4.html`). Diagram 1 (Before) sits after *The problem*; Diagram 2 (With Follow) after the insights. Only motion is the subtle read/write packets in the With-Follow variant (reduced-motion off).
  - **IA restructure:** research methodology (spine + 6 engagements) **clubbed under the timeline** (the journey); a new **"Insights & areas of opportunity"** section (the 3 deck insights + guilt quote) sits between the two diagrams. Narrative: hero → journey+research → problem → Before diagram → insights → With-Follow diagram → how-it-works pipeline reel (kept) → differentiators → four questions → where-it-sits → demo.
  - **Bolder hero:** giant `Follow.` display lockup (Bricolage 800, clamp 76–156px, orange period) as the h1, above the existing headline/sub.
  - **Motion phase begun** (was deferred in DESIGN.md §7/§10): an `AmbientField` backdrop (soft drifting orbs, pointer + scroll parallax, reduced-motion-still) plus a glass hover-lift on the differentiator/insight cards. Subtle/clean per the user; reuses locked tokens + Follow burnt-orange; no new deps (`motion@12` still unused).
- DESIGN-PHASE-1 v3 (2026-06-27): per user feedback — (1) the ambient promoted from hero-scoped to a **page-wide `position:fixed` backdrop** (z-index 0) with all content on a `.pageContent` z-index:1 layer, so the soft background runs through the whole page (Material-style layering); (2) the two compare diagrams **refined** — glass-elevated panels, the Follow Index now names its 4 pillars, the team is named (Maya/Alex/Sam), consistent strokes/spacing; (3) a **transactive-memory-system definition aside** (`.defNote`, textbook-style) added to the problem section; (4) the **"four questions" section removed** (+ dead `.queries/.q` CSS; DemoCallout copy reworded off it). `next build` clean; verified live.
- DESIGN-PHASE-1 v4 (2026-06-27): narrative + motion overhaul, per user. New flow: **hero → timeline+research → ONE white block (`.blockWhite`) holding {problem+TMS · the BEFORE diagram · insights} → HMW statement → Follow concept reveal (big `Follow.` + the WITH-FOLLOW diagram, with the old "where it sits"/Glean/MCP copy folded into its note) → how it works (pipeline reel) → demo → "what it accomplishes" (the old differentiators, reworded)**. Diagrams **reordered** to `web→AI→user→meeting` (before) and `web→AI→Follow Index→user` (after) — meeting last; variant-specific layouts. **Ambient** now drifts **side-to-side** and carries **blue (`#1C39BB`) + orange** orbs whose opacity cross-fades by scroll (intro=both → problem=orange → Follow=blue) for a cleaner colour arc. **Pipeline reel** base frame fixed to start **blank** (`.ed { opacity:0 }` until `.fp3`; was leaking `.ed.con` + the `.ed.sps` arrow marker). `next build` clean (4.72kB); flow + diagram order + reel base + console all verified live.
- GH-DECK-1 (2026-06-30): bespoke `/work/greener-hours` built as a **deck-as-page** (resolves D-06). The finished presentation deck was vendored verbatim to `public/greener-hours/index.html` (Next serves it at `/greener-hours/index.html`) and embedded in a 16:9 iframe (`aspect-ratio:16/9`, height-capped to the viewport) so the self-scaling 1920×1080 canvas fills the frame with no letterbox; a mono "Open full screen ↗" link opens the raw deck. The page reuses the `[slug]` project pager (← Follow / Healthy Materials →) and sets `--accent` from `lib/projects.ts`. `"greener-hours"` added to `BESPOKE_SLUGS` so the dynamic route no longer emits it (no duplicate). The deck is a **vendored asset** — its markup/CSS/JS, fonts, and amber palette are untouched; no design tokens applied to it. `lib/projects.ts` left as-is (card accent Forest `#1C3B36`, status `SIMULATED`). The interactive prototype embed is the deferred follow-up.
- GH-PAGE-1 (2026-06-30): **retired the iframe deck-as-page; rebuilt `/work/greener-hours` as a bespoke narrative case study** (reverses D-06). Hybrid look (Bricolage + scoped Fraunces display + the deck's amber/navy in the diagrams; Forest primary accent), CURATED product (3-surface overview + full Tier-1 chat mockup). New files: `lib/greenerHours.ts`; `app/work/greener-hours/{AmbientField, ScaleChart, VisibilityFlow, ForceVisual, HeadersDiagram, AdoptionCurve, SurfaceThumbs, ComputeWindowMock}` + `diagrams.css`; rewrote `page.tsx` + `greener-hours.module.css`. `lib/projects.ts` status `SIMULATED` → `COURSE PROJECT · CONCEPT` (Forest accent unchanged). The vendored deck is kept + linked ("View the full deck ↗"). `next build` clean (20/20; `/work/greener-hours` static 3.5 kB); verified in-browser (Fraunces scoped, amber/navy/forest semantics, 3 dark bands, no h-overflow desktop+mobile, no console errors).
- GH-PAGE-1 restructure (2026-06-30, same day): per Rishabh — reordered so the HTTPS precedent + 3 supporting precedents sit **right after the reframe** (one combined dark "the reframe, proven" section), then opportunity → product; the cramped reframe quote restyled to a clean full-width opener; and the product became the **three surfaces in a tabbed view** (`TierTabs`) with all three fully rebuilt (`ComputeWindowMock` T1, `SchedulerMock` T2, `DashboardMock` T3 — superseding the curated one-full-two-thumbnails approach; `SurfaceThumbs` removed). Build clean (8.54 kB); DOM-verified + 3-lens review fixes applied.
- GH-PAGE-1 come-alive (2026-06-30, same day): made the three tier surfaces **LIVE** (per Rishabh + the supplied `greener-hours-prototype.jsx`), adapted into the page's tokens with no new deps. Implemented `app/api/ask` as a real **server-side Claude proxy** (per-IP rate limiting; `ANTHROPIC_API_KEY` server-only — was a 501 scaffold) and wired the Tier-1 chat to it; Tier-2 = a working scheduler queue, Tier-3 = a ticking live dashboard. `DemoCallout` status `SIMULATED` → **`WORKING`** — **resolves D-03 for Greener Hours** (project framing stays `COURSE PROJECT · CONCEPT`; the two axes are distinct). Also restyled the section backgrounds: §3+§4 linked on one dark band, §5 reframe centered on light, §6 opportunity on a blue band. Build clean (10.1 kB; `/api/ask` server route); verified live — chat returns a graceful 503 without the key, scheduler/dashboard interactive, no console/overflow. **Prod chat needs `ANTHROPIC_API_KEY` in Railway.**
- SHELL-1.1 + ABOUT-PAGE-1 (2026-07-01): per Rishabh — the flagship pages' shared design DNA promoted into the shell, the About page built for real, and the shell surfaces brought onto the newer language.
  - **Design tokens v1.1** (`globals.css`): added `--shadow-rest`/`--shadow-lift` (the flagship elevation recipes), `--glass-bg`/`--glass-blur`, `--nav-h: 60px`, plus global `.kicker` (mono-gold section heading) and opt-in `.lift` (pointer-gated hover-lift). **All v1.0 tokens/utilities unchanged**; default accent stays Persian Blue (the person/site accent — About confirms it deliberately).
  - **Shared `components/AmbientField`** — the HW-generation engine (scroll-phase Lissajous, `[data-ambient-dim]`, `document.hidden` guard) parameterized by orb colors/alphas + rest opacities. Used by `/` and `/about` (gold→Persian-Blue), `/archive` (gold-led), and each `/archive/[slug]` (**project accent + gold** — per-project themed backgrounds for the older projects). The four flagship pages keep their own tuned copies, untouched.
  - **Shared `components/DriftGroup`** — the MaterialRail scroll-parallax recipe generalized (`data-depth` children); used on the home arc words + earlier-work grid, the archive grid, and the About photo cluster.
  - **`/about` rebuilt** from the approved sandbox build (resolves D-04): thesis hero + placeholder photo cluster (kept as placeholders), journey SVG timeline, CV-as-work-list with CTAs into the site, skills panels, contact. Accent = global Persian Blue (no page override). SVG text uses CSS classes for `font-family` (next/font hashes family names — literal names in SVG attrs would silently fall back).
  - **Home reworked onto v1.1**: AmbientField + z-layered content, hero kicker → gold (was accent-blue), arc band → frosted glass, card hovers → `--shadow-lift`. Copy untouched (D-01 still open).
  - **Breadcrumbs** (`components/Breadcrumbs`, mounted in `layout.tsx` under `<Nav/>`): mono location trail on every route except `/`; deliberately **non-sticky** so it adds no permanent chrome height (the Follow reel pins captions assuming ~60px of chrome); labels from `lib/projects.ts` (FLAGSHIPS + ARCHIVE cover all slugs).
  - **Footer contact links live** (GitHub/Behance per the About handles; LinkedIn placeholder dropped).
- HM-PROTO-1 (2026-07-02): `/work/healthy-materials` §9 SwapCard → the **working `PackagesApp`** (browser-framed packages configurator: 4 scopes × 20 lines on illustrative-but-realistic data with on-product honesty labels, live cost/carbon/health totals, cost-pressure lens, CSV spec-sheet export, grounded assistant) + standalone `/work/healthy-materials/prototype`. **Resolves D-03 for Healthy Materials: demo = WORKING** (project framing stays `GRADUATE RESEARCH · CONCEPT`; the two axes are distinct, as with GH). `/api/ask` now prefers **OpenRouter** (`OPENROUTER_API_KEY`, default model `anthropic/claude-haiku-4.5`) over direct Anthropic, with demo-keyed server-side prompts + bounded grounding context — the GH Tier-1 chat rides the same route unchanged. Data-honesty rule for the demo changed per Rishabh from "directional only, no numbers" to "mock data simulating the real thing accurately, labeled illustrative on-product".
- HW-PROTO-1 (2026-07-02): `/work/housing-works` scene 9 WorkshopHarness → the **working `WorkshopsApp`** ("Trustee Workshops": needs queue → explainable scored matching → 45-min session capture loop → searchable archive + "Ask the archive" assistant on `/api/ask` demo `hw-workshops`) + standalone `/work/housing-works/prototype`. **Resolves D-03 for Housing Works: demo = WORKING**; `lib/projects.ts` status `SIMULATED` → `STUDIO PROJECT · CONCEPT` (framing badge — demo status lives on the DemoCallout, same axes split as GH/HM). Data honesty: illustrative stand-ins labeled on-product; matcher explicitly labeled real-computation-not-model-output. The spec's "Phase B live harness" plan is superseded.
- FOLLOW-DEMO-1 (2026-07-02): the **team-memory sandbox is BUILT and live** on `/work/follow` (+ `/work/follow/prototype`) — pre-loaded 3-person workspace (three different AI tools), 20 provenance-carrying entries, 2 contested pairs flagged not resolved, who-knows-what directory, and Ask Follow grounded on the memory via `/api/ask` demo `follow`. **Closes D-02's open follow-up (the `href="#"` CTA) and resolves D-03 for Follow: demo = WORKING**; `lib/projects.ts` status `SIMULATED` → `CAPSTONE · WORKING DEMO` (framing badge — the sandbox is genuinely working regardless of the separate "present the shipped product as working" question, which stays with Rishabh). All flagship flip points in sync (the old home-page Follow callout no longer exists).
- GH-SIM-1 (2026-07-02): the three Greener Hours surfaces now run on **one shared simulation** (`GhSim` provider in `TierTabs`): one clock/grid/queue — T2 pause freezes the T1 pill, T2 jobs surface on a new T3 flex strip, T1 chat is grounded with the live sim state + chips. DemoCallout CTA → new `/work/greener-hours/prototype` (deck stays linked in the hero).
- FREE-MODELS-1 (2026-07-02, per Rishabh "free models or pro models under a dollar or 2 max"): `/api/ask` OpenRouter calls use the `models` fallback array `gpt-oss-120b:free → llama-3.3-70b:free → gemini-2.5-flash-lite` ($0.10/$0.40 MTok paid safety net; verified against live pricing); `OPENROUTER_MODEL` prepends. Anthropic direct fallback `sonnet-4-6` → `haiku-4-5`.
- FOLLOW-MCP-1 (2026-07-02): the Follow sandbox gained the **MCP console** — a real model running a real tool-calling loop over Follow's ACTUAL MCP tool contracts (names/schemas/response shapes read from the workspace-platform repo and verified against the live connector), executed on the sandbox workspace with the write path live (save_conversation → a visible "You" entry). Tool registry is server-side in `/api/ask` (`demo:"follow-mcp"`); OpenRouter-only (labeled 503 otherwise). The public repo is linked from the console ("source: workspace-platform ↗").
- WORLD-READY-1 (2026-07-03): full-site review (graded report at `Desktop\Claude\REVIEW-PORTFOLIO-2026-07-03.md`) → the publication layer shipped (metadata/OG/icons/robots/sitemap/JSON-LD/branded 404+error/skip link), contrast tokens darkened (gold #7a5f3c, HM sage #4F6B33), `/api/ask` hardened (timeouts, last-hop XFF + eviction, body cap, generic client errors, refuse-not-fabricate prompts), trust-copy batch (PGDM→MS on the Follow hero, participants softened pending the true n, Lockheed qualified, en-US sweep), MaterialRail rAF leak fixed via DriftGroup, 31 images recompressed (−4.7MB) + sharp installed, ProjectPager on all four flagships (wrap-around), home availability chip + card taglines (**partially resolves D-01: the site now states "hire-me design engineer" above the fold; Follow stays the founder-signal**), dead code purged (~3.2K lines) + ESLint real. **Still open for Rishabh:** custom domain purchase, About portraits, LinkedIn URL, fresher CV PDF, OpenRouter spend cap, the true Follow concept-test n (restore specificity when confirmed).
- WORLD-READY-2 (2026-07-03): four of those opens closed per Rishabh — Follow research n restored (5 concept tests = 4 peers + Alexandra Becker's session; 2 experts Andy + Atanu Sinha; page numbers now consistent end-to-end); `rishabhsalian.com` live on Railway → `lib/site.ts` fallback + all canonicals/OG/sitemap point at it; LinkedIn (`linkedin.com/in/rishabh-salian-000000`, verified loading) added via new `SOCIALS` single-source to Footer/About/JSON-LD; `/api/ask` free chain → best-intelligence tools-capable free tier (nemotron-3-ultra-550b → nemotron-3-super-120b → gpt-oss-120b → gemini-2.5-flash-lite paid net). REMAINING open: About portraits, CV refresh, OpenRouter dashboard spend cap, LinkedIn profile location says Mumbai (update to NYC).
