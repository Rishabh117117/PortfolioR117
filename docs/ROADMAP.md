# Roadmap
- Phase 0 — Foundation: clone repo, skeleton, planning docs, Railway deploy. DONE.
- Phase 1 — Design language + site shell: locked design tokens (docs/DESIGN.md), Nav/Footer chrome, homepage shell, section routes (work/about/cv/archive), reusable ProjectCard + DemoCallout, micro-transitions, reduced-motion + a11y. DONE.
- Phase 2 — Site content + archive: concierge shell, archive gallery from PDF extraction, real copy, design refinements. IN PROGRESS.
- Phase 3 — Flagship project pages + interactive demos. IN PROGRESS. Bespoke pages built: `/work/housing-works`, `/work/healthy-materials`, `/work/follow` (FOLLOW-PAGE-1 — inline autoplay-on-view pipeline reel + deck timeline + research; RESTRUCTURE-1 + DESIGN-PHASE-1 added the inline two-up and the before/after "structural change" morph reel; demo = illustrative animation, SIMULATED), and `/work/greener-hours` (GH-PAGE-1 — **bespoke narrative case study** rebuilt from the presentation deck: hybrid treatment (Bricolage shell + scoped Fraunces display + the deck's amber/navy carried into faithfully-rebuilt SVG diagrams), the scale chart, dark-matter flow, three surfaces with a full Tier-1 chat mockup, the API-headers diagram, the HTTPS precedent, precedents grid, honest trade-offs + speculative KPIs; status `COURSE PROJECT · CONCEPT`. The original deck stays vendored at `public/greener-hours/index.html` and is linked as "View the full deck ↗". Superseded GH-DECK-1's iframe deck-as-page). Remaining: the Greener Hours interactive prototype embed (its `/api/ask`-routed Tier-1 chat → would flip the badge to LIVE API); the Follow team-memory sandbox; live AI demos.
- Phase 4 — Live API integration: **`/api/ask` proxy IMPLEMENTED** (server-side Claude proxy + per-IP rate limiting; powers the live Greener Hours Tier-1 chat — needs `ANTHROPIC_API_KEY` in env). Remaining: the concierge, streaming, a durable rate-limit store, optional Electricity Maps.
