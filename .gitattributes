# Normalize line endings: text files stored and checked out as LF.
# Keeps commits deterministic regardless of each machine's core.autocrlf.
* text=auto eol=lf

# Binary assets — never touched, never diffed.
*.png  binary
*.jpg  binary
*.jpeg binary
*.pdf  binary
*.ico  binary
*.glb  binary
